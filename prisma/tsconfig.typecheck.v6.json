{
  "//": "Same as tsconfig.typecheck.json, but resolves the `./test-setup*` imports to the Prisma v6 fixtures so the suites are type-checked against both supported client majors. Requires `npm run prisma:generate` first.",
  "extends": "./tsconfig.typecheck.json",
  "compilerOptions": {
    "moduleSuffixes": [".v6", ""]
  }
}
