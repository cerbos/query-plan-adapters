{
  "allowed": [
    "a1",
    "a3",
    "a4",
    "a6",
    "a7",
    "a8",
    "a9",
    "b1",
    "b3",
    "b5",
    "h4",
    "j2"
  ],
  "degenerateReason": null,
  "id": "composition/allow-and-deny/conditional-deny",
  "intent": "A conditional ALLOW (aBool == true) plus a conditional DENY (aNumber > 10), planned as and(allow, not(deny)).",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aNumber"
                      },
                      {
                        "value": 10
                      }
                    ],
                    "operator": "gt"
                  }
                }
              ],
              "operator": "not"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aBool"
                },
                {
                  "value": true
                }
              ],
              "operator": "eq"
            }
          }
        ],
        "operator": "and"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": {
    "issue": "https://github.com/cerbos/query-plan-adapters/issues/530",
    "pdp": [
      "0.54.0",
      "0.55.0"
    ],
    "reason": "j2 has no aNumber, and the two calls answer different questions about it. The plan request leaves aNumber unknown, so the plan carries the DENY as not(gt(aNumber, 10)), which over a NULL column denies j2: the expected answer for a column-backed store. The per-row check() call sends aNumber as absent, so the DENY condition errors and check() treats it as not matching (known PDP behaviour): the ALLOW (aBool == true) grants j2. No adapter can match the recorded decision."
  },
  "request": {
    "action": "composition/allow-and-deny/conditional-deny",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "R.attr.aBool == true"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    },
    {
      "condition": {
        "match": {
          "expr": "R.attr.aNumber > 10"
        }
      },
      "effect": "EFFECT_DENY",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "extended",
  "trap": "The DENY arrives as a negated subtree; dropping it or mishandling NULL under the not() grants rows the DENY removes."
}
