{
  "allowed": [
    "a1",
    "a6",
    "b2",
    "h4",
    "j2"
  ],
  "degenerateReason": null,
  "id": "composition/variable/allow-and-deny-through-variables",
  "intent": "ALLOW on V.compose_teamed (aOptionalString in P.attr.fewTeams) and DENY on V.compose_large (aNumber > 10), planned as and(not(gt), in).",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aNumber"
                      },
                      {
                        "value": 10
                      }
                    ],
                    "operator": "gt"
                  }
                }
              ],
              "operator": "not"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aOptionalString"
                },
                {
                  "value": [
                    "set",
                    "",
                    "%_o"
                  ]
                }
              ],
              "operator": "in"
            }
          }
        ],
        "operator": "and"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": {
    "issue": "https://github.com/cerbos/query-plan-adapters/issues/530",
    "pdp": [
      "0.54.0",
      "0.55.0"
    ],
    "reason": "j2 has no aNumber, and the two calls answer different questions about it. The plan request leaves aNumber unknown, so the plan carries the DENY as not(gt(aNumber, 10)), which over a NULL column denies j2: the expected answer for a column-backed store. The per-row check() call sends aNumber as absent, so V.compose_large errors and check() treats the DENY as not matching (known PDP behaviour): the ALLOW grants j2. No adapter can match the recorded decision."
  },
  "request": {
    "action": "composition/variable/allow-and-deny-through-variables",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "V.compose_teamed"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    },
    {
      "condition": {
        "match": {
          "expr": "V.compose_large"
        }
      },
      "effect": "EFFECT_DENY",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "extended",
  "trap": "The planner expands variables; a wrong expansion or a dropped DENY lets h1/h2/h3/h5 through."
}
