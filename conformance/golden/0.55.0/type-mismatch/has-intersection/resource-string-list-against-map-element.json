{
  "allowed": [],
  "degenerateReason": "The literal's only element is a map, which no string in tagNames equals, so CEL denies every seed.",
  "id": "type-mismatch/has-intersection/resource-string-list-against-map-element",
  "intent": "hasIntersection of a string list attribute with a literal list whose only element is a map (hasIntersection(tagNames, [{\"a\": 1}])).",
  "oracle": "empty",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "variable": "request.resource.attr.tagNames"
          },
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "expression": {
                          "operands": [
                            {
                              "value": "a"
                            },
                            {
                              "value": 1
                            }
                          ],
                          "operator": "set-field"
                        }
                      }
                    ],
                    "operator": "struct"
                  }
                }
              ],
              "operator": "list"
            }
          }
        ],
        "operator": "hasIntersection"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/has-intersection/resource-string-list-against-map-element",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "hasIntersection(R.attr.tagNames, [{\"a\": 1}])"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "No string equals a map; binding the map as a serialised string or a coerced value matches rows."
}
