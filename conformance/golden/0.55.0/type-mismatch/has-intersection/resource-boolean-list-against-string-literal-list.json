{
  "allowed": [
    "a1"
  ],
  "degenerateReason": null,
  "id": "type-mismatch/has-intersection/resource-boolean-list-against-string-literal-list",
  "intent": "hasIntersection(aBoolList, [\"true\"]) OR aNumber == 5: a boolean list against a string-only literal list.",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aBoolList"
                },
                {
                  "value": [
                    "true"
                  ]
                }
              ],
              "operator": "hasIntersection"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aNumber"
                },
                {
                  "value": 5
                }
              ],
              "operator": "eq"
            }
          }
        ],
        "operator": "or"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/has-intersection/resource-boolean-list-against-string-literal-list",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "hasIntersection(R.attr.aBoolList, [\"true\"]) || R.attr.aNumber == 5"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "No element has the right type, so CEL answers false; a coercing store matches every list holding true."
}
