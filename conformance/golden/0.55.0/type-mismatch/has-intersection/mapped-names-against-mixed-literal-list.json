{
  "allowed": [
    "a1",
    "a3",
    "a5",
    "a6",
    "a8"
  ],
  "degenerateReason": null,
  "id": "type-mismatch/has-intersection/mapped-names-against-mixed-literal-list",
  "intent": "hasIntersection(tags.map(t, t.name), [\"public\", 0]): projected tag names against a literal list mixing a string and a number.",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.tags"
                },
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "t.name"
                      },
                      {
                        "variable": "t"
                      }
                    ],
                    "operator": "lambda"
                  }
                }
              ],
              "operator": "map"
            }
          },
          {
            "value": [
              "public",
              0
            ]
          }
        ],
        "operator": "hasIntersection"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/has-intersection/mapped-names-against-mixed-literal-list",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "hasIntersection(R.attr.tags.map(t, t.name), [\"public\", 0])"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "\"public\" matches and 0 cannot in CEL; MySQL coerces names to 0 and adds every other tagged row, and H2 fails to execute."
}
