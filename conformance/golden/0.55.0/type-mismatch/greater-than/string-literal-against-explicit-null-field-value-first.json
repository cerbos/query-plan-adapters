{
  "allowed": [
    "a6",
    "b2",
    "b3",
    "c1",
    "d1"
  ],
  "degenerateReason": null,
  "id": "type-mismatch/greater-than/string-literal-against-explicit-null-field-value-first",
  "intent": "\"m\" > owner: a string literal ordered against an attribute that is an explicit null on some rows, value first.",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "value": "m"
          },
          {
            "variable": "request.resource.attr.owner"
          }
        ],
        "operator": "gt"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/greater-than/string-literal-against-explicit-null-field-value-first",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "\"m\" > R.attr.owner"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "Ordering a null is a CEL error, so the null-owner rows are denied; a store that orders null below every string returns them."
}
