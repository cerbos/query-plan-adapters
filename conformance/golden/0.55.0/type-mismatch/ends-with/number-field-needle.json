{
  "allowed": [],
  "degenerateReason": "A string function over a non-string operand is a CEL type error that denies every seed. A store coercing the number to text would return rows.",
  "id": "type-mismatch/ends-with/number-field-needle",
  "intent": "endsWith() on a string attribute with a numeric attribute as the suffix (R.attr.aString.endsWith(R.attr.aNumber)).",
  "oracle": "empty",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "variable": "request.resource.attr.aString"
          },
          {
            "variable": "request.resource.attr.aNumber"
          }
        ],
        "operator": "endsWith"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/ends-with/number-field-needle",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "R.attr.aString.endsWith(R.attr.aNumber)"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "A non-string needle is a CEL error; building the LIKE pattern from the number coerces it to text and returns rows."
}
