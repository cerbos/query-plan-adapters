{
  "allowed": [
    "a3",
    "a8"
  ],
  "degenerateReason": null,
  "id": "type-mismatch/less-or-equal/negated-number-field-against-string-literal-value-first",
  "intent": "!(\"5\" <= aNumber) OR aNumber == 2: a negated ordering between a numeric string and a number attribute, value first.",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "value": "5"
                      },
                      {
                        "variable": "request.resource.attr.aNumber"
                      }
                    ],
                    "operator": "le"
                  }
                }
              ],
              "operator": "not"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aNumber"
                },
                {
                  "value": 2
                }
              ],
              "operator": "eq"
            }
          }
        ],
        "operator": "or"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/less-or-equal/negated-number-field-against-string-literal-value-first",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "!(\"5\" <= R.attr.aNumber) || R.attr.aNumber == 2"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "The ordering is a CEL error, and so is its negation, so only a3/a8 are allowed; a store that orders strings above numbers answers false and the negation returns every row, and a coercing store adds each aNumber < 5."
}
