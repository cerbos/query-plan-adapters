{
  "allowed": [
    "a6"
  ],
  "degenerateReason": null,
  "id": "null/in/null-literal-in-number-list",
  "intent": "null in aNumberList: true only where a list holds a null element (a6 [null, 2]).",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "value": null
          },
          {
            "variable": "request.resource.attr.aNumberList"
          }
        ],
        "operator": "in"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "null/in/null-literal-in-number-list",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "null in R.attr.aNumberList"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "A null element is a value in CEL; MySQL's JSON_TABLE turns it into SQL NULL (which = never matches) and Elasticsearch does not index it, so a6 is lost."
}
