{
  "allowed": [
    "a3"
  ],
  "degenerateReason": null,
  "id": "null/in/missing-attribute-in-single-element-list",
  "intent": "Membership of a missing-when-null attribute in a one-element literal list.",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "variable": "request.resource.attr.aOptionalString"
          },
          {
            "value": "x"
          }
        ],
        "operator": "eq"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "null/in/missing-attribute-in-single-element-list",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "R.attr.aOptionalString in [\"x\"]"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "A missing attribute is a CEL error that denies; coalescing NULL, or simplifying the one-element IN, must not let those rows through."
}
