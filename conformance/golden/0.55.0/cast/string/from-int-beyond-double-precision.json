{
  "allowed": [
    "i6"
  ],
  "degenerateReason": null,
  "id": "cast/string/from-int-beyond-double-precision",
  "intent": "string(int(aString)) == \"9007199254740993\": an int past 2^53 rendered back to a string.",
  "oracle": "discriminating",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aString"
                      }
                    ],
                    "operator": "int"
                  }
                }
              ],
              "operator": "string"
            }
          },
          {
            "value": "9007199254740993"
          }
        ],
        "operator": "eq"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "cast/string/from-int-beyond-double-precision",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "string(int(R.attr.aString)) == \"9007199254740993\""
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "i6 holds 2^53 + 1, which CEL converts exactly and renders in full; an evaluator that carries the int as a double renders 2^53 instead, and one that cannot render a big int errors and under-grants i6."
}
