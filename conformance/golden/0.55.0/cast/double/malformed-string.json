{
  "allowed": [],
  "degenerateReason": "No seed's aString converts to a number, so double(R.attr.aString) is a conversion error for every seed and denies.",
  "id": "cast/double/malformed-string",
  "intent": "double() over a string attribute compared with a threshold.",
  "oracle": "empty",
  "pdp": "0.55.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aString"
                }
              ],
              "operator": "double"
            }
          },
          {
            "value": 50
          }
        ],
        "operator": "gt"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "cast/double/malformed-string",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "double(R.attr.aString) > 50.0"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "CEL requires the whole string to be a number; SQL CAST reads leading digits, so a direct cast wrongly returns a2, a3 and b2."
}
