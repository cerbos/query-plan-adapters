{
  "allowed": [
    "h1",
    "h3"
  ],
  "degenerateReason": null,
  "id": "regex/matches/dot-excludes-newline",
  "intent": "matches(\"^a.*b$\"), where . must not cross a newline.",
  "oracle": "discriminating",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "variable": "request.resource.attr.aString"
          },
          {
            "value": "^a.*b$"
          }
        ],
        "operator": "matches"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "regex/matches/dot-excludes-newline",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "R.attr.aString.matches(\"^a.*b$\")"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "In RE2 . does not match \\n, so h2 (\"a\\nb\") is denied; dialects where . matches a newline over-grant it."
}
