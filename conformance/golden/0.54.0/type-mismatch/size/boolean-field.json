{
  "allowed": [],
  "degenerateReason": "size() is not defined on bool, so every seed is a CEL error and denies. A store that measures the stored value would return rows.",
  "id": "type-mismatch/size/boolean-field",
  "intent": "size() of a boolean attribute (size(R.attr.aBool) > 0).",
  "oracle": "empty",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aBool"
                }
              ],
              "operator": "size"
            }
          },
          {
            "value": 0
          }
        ],
        "operator": "gt"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/size/boolean-field",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "size(R.attr.aBool) > 0"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "size() is not defined on bool, so CEL denies; measuring the stored value (LENGTH of 'true', array length) returns rows."
}
