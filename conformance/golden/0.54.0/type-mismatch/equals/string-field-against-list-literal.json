{
  "allowed": [
    "a1"
  ],
  "degenerateReason": null,
  "id": "type-mismatch/equals/string-field-against-list-literal",
  "intent": "A string attribute compared with a one-element list literal, OR'd with aNumber == 5.",
  "oracle": "discriminating",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aString"
                },
                {
                  "value": [
                    "same"
                  ]
                }
              ],
              "operator": "eq"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aNumber"
                },
                {
                  "value": 5
                }
              ],
              "operator": "eq"
            }
          }
        ],
        "operator": "or"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/equals/string-field-against-list-literal",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "R.attr.aString == [\"same\"] || R.attr.aNumber == 5"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "A string never equals a list, so only a1 is allowed; unwrapping the one-element list compares aString with \"same\" and over-grants a9."
}
