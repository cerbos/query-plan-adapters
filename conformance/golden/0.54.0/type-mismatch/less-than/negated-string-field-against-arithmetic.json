{
  "allowed": [
    "a3",
    "a8"
  ],
  "degenerateReason": null,
  "id": "type-mismatch/less-than/negated-string-field-against-arithmetic",
  "intent": "!(aString < aNumber + 1.0) OR aNumber == 2: a negated ordering between a string attribute and an arithmetic result.",
  "oracle": "discriminating",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aString"
                      },
                      {
                        "expression": {
                          "operands": [
                            {
                              "variable": "request.resource.attr.aNumber"
                            },
                            {
                              "value": 1
                            }
                          ],
                          "operator": "add"
                        }
                      }
                    ],
                    "operator": "lt"
                  }
                }
              ],
              "operator": "not"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aNumber"
                },
                {
                  "value": 2
                }
              ],
              "operator": "eq"
            }
          }
        ],
        "operator": "or"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/less-than/negated-string-field-against-arithmetic",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "!(R.attr.aString < R.attr.aNumber + 1.0) || R.attr.aNumber == 2"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "CEL has no string-vs-int ordering, so the comparison is an error under either polarity and only a3/a8 are allowed; a translator that cannot type the arithmetic result lets the store compare as text (SQLite) or order strings above numbers (MongoDB), and the negation returns rows."
}
