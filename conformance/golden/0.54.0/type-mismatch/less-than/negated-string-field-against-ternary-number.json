{
  "allowed": [
    "a3",
    "a8"
  ],
  "degenerateReason": null,
  "id": "type-mismatch/less-than/negated-string-field-against-ternary-number",
  "intent": "!(aString < (aBool ? 1 : 2)) OR aNumber == 2: a negated ordering between a string attribute and a ternary whose arms are numbers.",
  "oracle": "discriminating",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aString"
                      },
                      {
                        "expression": {
                          "operands": [
                            {
                              "variable": "request.resource.attr.aBool"
                            },
                            {
                              "value": 1
                            },
                            {
                              "value": 2
                            }
                          ],
                          "operator": "if"
                        }
                      }
                    ],
                    "operator": "lt"
                  }
                }
              ],
              "operator": "not"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "request.resource.attr.aNumber"
                },
                {
                  "value": 2
                }
              ],
              "operator": "eq"
            }
          }
        ],
        "operator": "or"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "type-mismatch/less-than/negated-string-field-against-ternary-number",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "!(R.attr.aString < (R.attr.aBool ? 1 : 2)) || R.attr.aNumber == 2"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "Either arm is a number, so the ordering is a CEL error under either polarity and only a3/a8 are allowed; checking orderability only on a literal operand misses the ternary, and a store that orders strings above numbers answers false and the negation returns every row."
}
