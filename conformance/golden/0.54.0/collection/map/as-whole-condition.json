{
  "allowed": [],
  "degenerateReason": "map() returns a list, not a boolean, so the condition is not evaluable and CEL denies every seed.",
  "id": "collection/map/as-whole-condition",
  "intent": "map() used as the entire condition, returning a list rather than a boolean.",
  "oracle": "empty",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "variable": "request.resource.attr.tags"
          },
          {
            "expression": {
              "operands": [
                {
                  "variable": "t.name"
                },
                {
                  "variable": "t"
                }
              ],
              "operator": "lambda"
            }
          }
        ],
        "operator": "map"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "collection/map/as-whole-condition",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "R.attr.tags.map(t, t.name)"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "There is no boolean meaning and check() denies every row; treating a non-empty projection as true over-grants. It must be rejected."
}
