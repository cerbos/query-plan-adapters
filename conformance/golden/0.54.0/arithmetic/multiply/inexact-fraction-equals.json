{
  "allowed": [],
  "degenerateReason": "Neither product is exact in IEEE double, so neither equation holds for any seed. Exact-decimal arithmetic on the store would return the aNumber = 3 seeds, and an algebraic solve a4.",
  "id": "arithmetic/multiply/inexact-fraction-equals",
  "intent": "Equality over an attribute times a non-representable fraction (aNumber * 0.1 == 0.3 || aDouble * 3.0 == 3.9).",
  "oracle": "empty",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aNumber"
                      },
                      {
                        "value": 0.1
                      }
                    ],
                    "operator": "mult"
                  }
                },
                {
                  "value": 0.3
                }
              ],
              "operator": "eq"
            }
          },
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aDouble"
                      },
                      {
                        "value": 3
                      }
                    ],
                    "operator": "mult"
                  }
                },
                {
                  "value": 3.9
                }
              ],
              "operator": "eq"
            }
          }
        ],
        "operator": "or"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "arithmetic/multiply/inexact-fraction-equals",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "R.attr.aNumber * 0.1 == 0.3 || R.attr.aDouble * 3.0 == 3.9"
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "3 * 0.1 is not 0.3 and 1.3 * 3.0 is 3.9000000000000004 in IEEE doubles; exact-decimal store arithmetic (MySQL) returns the aNumber = 3 seeds, and solving to aDouble == 3.9 / 3.0, which is exactly 1.3, returns a4."
}
