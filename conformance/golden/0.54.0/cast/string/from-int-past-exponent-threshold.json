{
  "allowed": [
    "i1"
  ],
  "degenerateReason": null,
  "id": "cast/string/from-int-past-exponent-threshold",
  "intent": "string(int(aDouble)) == \"1000000\": an int of 1e6 rendered as a string.",
  "oracle": "discriminating",
  "pdp": "0.54.0",
  "plan": {
    "condition": {
      "expression": {
        "operands": [
          {
            "expression": {
              "operands": [
                {
                  "expression": {
                    "operands": [
                      {
                        "variable": "request.resource.attr.aDouble"
                      }
                    ],
                    "operator": "int"
                  }
                }
              ],
              "operator": "string"
            }
          },
          {
            "value": "1000000"
          }
        ],
        "operator": "eq"
      }
    },
    "kind": "KIND_CONDITIONAL"
  },
  "planError": null,
  "plannerDivergence": null,
  "request": {
    "action": "cast/string/from-int-past-exponent-threshold",
    "resourceKind": "conformance"
  },
  "rules": [
    {
      "condition": {
        "match": {
          "expr": "string(int(R.attr.aDouble)) == \"1000000\""
        }
      },
      "effect": "EFFECT_ALLOW",
      "roles": [
        "USER"
      ]
    }
  ],
  "tier": "adversarial",
  "trap": "CEL renders an int in plain decimal, \"1000000\" (i1), where a double of the same value renders \"1e+06\"; rendering the int with the double formatting under-grants i1."
}
