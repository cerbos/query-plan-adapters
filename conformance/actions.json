{
  "description": "Every action defined in policies/adversarial.yaml, grouped against the Spring Data reference harness. 'conformance' actions must match the check() oracle. 'adapterUnsupported' lists conformance actions an adapter cannot express and must reject loudly. 'expectedUnsupported' lists shapes rejected by the reference; 'adapterSupportedExpected' records adapters that intentionally translate one of those shapes. 'nullRepresentationOmitted' lists actions that probe a `== null` comparison against an attribute the oracle OMITS for NULL columns; every adapter must translate these with its null representation set to 'omitted' and reject them. 'knownDivergences' records upstream planner behavior that prevents an oracle comparison. 'adapters' is the canonical adapter roster: every key of 'adapterUnsupported' and 'adapterSupportedExpected', and every key of an 'expectedUnsupported' entry's 'messages' map, must name one of these. Every throwing classification carries the substring that adapter's error must contain — 'message' on an 'adapterUnsupported' entry, 'messages.<adapter>' on an 'expectedUnsupported' one — so a harness proves the throw is the declared mechanism rather than a mapper typo or an unrelated validation.",
  "adapters": ["prisma", "mongoose", "drizzle", "convex", "langchain-chromadb", "sqlalchemy", "ent", "pgx", "elasticsearch-java", "spring-data", "activerecord"],
  "conformance": [
    "vf-le", "vf-ge", "vf-ne",
    "in-single", "in-empty",
    "like-percent", "like-underscore", "like-backslash",
    "unicode-eq", "empty-string-eq",
    "neg-number", "double-threshold",
    "all-on-empty", "exists-on-empty", "exists-one-multi", "not-exists",
    "outer-attr-depth2", "lambda-in-principal",
    "nary-and", "double-negation", "triple-negation", "not-empty",
    "optional-ne",
    "lambda-field-to-field", "field-to-field",
    "size-threshold", "size-filter-count", "string-size",
    "ternary-cmp", "ternary-expr-cond", "ternary-nested", "ternary-negated",
    "ternary-bare", "ternary-value-first", "ternary-null-cond",
    "f2f-contains", "f2f-startswith", "f2f-endswith",
    "arith-add", "arith-vf", "arith-sub", "arith-mult-neg",
    "arith-div", "arith-div-frac", "arith-both",
    "p-ternary-in-exists", "p-arith-in-lambda", "p-lambda-inner-f2f",
    "p-lambda-f2f-like", "p-size-nested",
    "p-ternary-of-ternaries", "p-ternary-vs-ternary", "p-ternary-under-all",
    "p-hasintersection-map", "p-deep-nest",
    "p-in-null-single", "p-in-null-multi", "p-startswith-concat",
    "p-struct", "p-not-exists-empty", "p-not-ternary-null", "p-double-frac",
    "n-not-exists-one-null", "n-all-mixed-null", "n-not-all-null", "n-not-all-absorb",
    "cr-contains", "cr-startswith", "cr-endswith", "cr-startswith-concat",
    "cr-div-zero", "cr-div-zero-ne", "cr-div-zero-eq-neg", "cr-size-frac-ge",
    "cr-div-neg-zero", "cr-div-other-column", "cr-div-then-add", "cr-div-then-add-ne",
    "w1-exists-chain", "w1-size-chain", "w1-in-chain", "w2-outer-relation",
    "w1-all-chain", "w1-not-exists-chain", "w1-size-zero-chain", "w1-size-nonneg-chain",
    "w1-not-in-chain", "w1-not-hasint-chain", "w1-not-size-chain",
    "w1-ternary-chain-cond", "w1-size-frac-chain", "w1-size-frac-le-chain",
    "rel-eq-hop", "rel-eq-num-hop", "rel-lt-hop", "rel-le-hop", "rel-gt-hop", "rel-ge-hop",
    "rel-range-hop", "rel-bool-hop", "rel-not-bool-hop", "rel-contains-hop", "rel-ne-null-hop",
    "rel-bool-hop2", "rel-startswith-hop2", "rel-hop-and-root", "rel-hop2-or-exists",
    "like-bracket", "cs-eq", "cs-contains", "cs-startswith", "cs-endswith",
    "arith-add-eq-frac", "arith-add-ne-frac", "arith-add-eq-frac-exact", "concat-f2f",
    "size-huge-gt", "size-huge-lt",
    "nan-ord-ternary", "nan-ord-ternary-vf", "nan-ord-le", "nan-ord-inf",
    "macro-depth3-exists", "macro-depth3-not-exists", "macro-depth3-all",
    "pv-exists", "pv-all",
    "hier-ancestor-ff", "hier-ancestor-cf",
    "hier-descendent-ff", "hier-descendent-cf",
    "hier-overlaps-ff", "hier-overlaps-cf",
    "hier-meta-like", "hier-meta-in", "hier-overlaps-meta", "hier-bracket",
    "cast-string-double", "cast-string-bool",
    "id-eq-const", "id-f2f", "id-f2f-ne", "id-concat", "id-concat-vf", "hier-list-id",
    "ts-window", "ts-vf", "ts-eq", "ts-eq-offset", "ts-ne",
    "in-null-elem-mixed", "in-null-elem-neg",
    "in-null-elem-only", "in-null-elem-only-neg",
    "in-null-elem-rel", "in-null-elem-rel-neg", "in-null-elem-hasint",
    "in-var-var", "in-var-var-neg",
    "null-eq", "null-ne", "vf-null-ne", "null-not-eq",
    "null-value-ne-const", "null-value-not-eq-const", "null-value-not-in-const",
    "null-value-f2f", "null-value-pv-not-exists"
  ],
  "adapterUnsupported": {
    "activerecord": [
      { "action": "ts-window", "reason": "The planner emits a nanosecond-precision now() literal, but ActiveRecord binds a Time into SQL at microsecond precision, so translating it would compare against a different instant than the policy specifies", "message": "carries sub-microsecond precision, which ActiveRecord truncates when binding a Time into SQL" },
      { "action": "ts-vf", "reason": "Same sub-microsecond now() limitation in value-first form", "message": "carries sub-microsecond precision, which ActiveRecord truncates when binding a Time into SQL" },
      { "action": "cr-div-other-column", "reason": "A denominator that is a second column may hold a zero, and IEEE-754 keeps the sign of a zero: 2.0 / -0.0 is -Infinity while 2.0 / 0.0 is +Infinity. SQL cannot tell the two apart — both satisfy `= 0` and no portable function reads the sign bit — so the adapter cannot pick the arm to fold. The existing cr-div-zero* shapes divide a column by ITSELF, where a zero denominator forces a zero numerator and the result is the sign-free NaN, which is why those translate and this one does not. spring-data assumes the positive reading and documents it (#312); this adapter refuses rather than guess", "message": "Cannot divide by a column that may be zero: IEEE-754 keeps the sign of a zero, SQL cannot tell -0.0 from 0.0, and thus the sign of the Infinity is unknown" },
      { "action": "cr-div-then-add", "reason": "The adapter keeps a zero-capable division as unresolved IEEE arms until a comparison folds them, because SQL has no NaN and no signed infinity to bind. Further arithmetic on those arms has no SQL form: NULL would propagate through the addition where CEL propagates NaN", "message": "add cannot take an operand that may be NaN or Infinity: only a comparison can resolve those values without binding them into SQL" },
      { "action": "cr-div-then-add-ne", "reason": "Same composed-division limitation as cr-div-then-add. This is the arm that proves it matters: `NaN != 2.0` is TRUE and the PDP allows the zero row, while `NULL + 1 <> 2` is UNKNOWN and drops it", "message": "add cannot take an operand that may be NaN or Infinity: only a comparison can resolve those values without binding them into SQL" },
      { "action": "cast-string-bool", "reason": "CAST(col AS TEXT) reproduces CEL's string() for a numeric or text column but not for a boolean: SQLite and MySQL have no boolean type and store 1/0, so the cast renders \"1\" where CEL renders \"true\" and the filter returns nothing while the PDP allows 14 of the 21 seeds. One translator serves SQLite, MySQL and PostgreSQL here; PostgreSQL alone would agree, and a rendering correct on one dialect and wrong on two is not one the adapter may choose. cast-string-double translates, because CAST over a numeric column agrees on all three", "message": "string() over a boolean column is not supported" }
    ],
    "ent": [
      { "action": "cast-string-bool", "reason": "CAST(... AS TEXT) reproduces CEL's string() for numeric and text columns but not for a boolean: SQLite and MySQL have no boolean type and store 1/0, so the cast renders \"1\" where CEL and PostgreSQL render \"true\". One translator serves all three engines this module targets, so a column the caller declares ValueBool fails closed rather than returning every matching row on one engine and none on another", "message": "string() over a boolean column is not supported" },
      { "action": "hier-list-id", "reason": "The descendant path is CONSTRUCTED by list() from a constant segment and the primary key rather than read from a column. `list` has never reached the wire from this corpus, so the translator has no case for it and the walk fails closed before the hierarchy operators around it are reached", "message": "unsupported operator: list" }
    ],
    "pgx": [
      { "action": "cast-string-bool", "reason": "Rejected by the same ValueBool guard as the ent module — the two share a byte-identical vendored translator. PostgreSQL alone would render CAST(a_bool AS TEXT) as \"true\" and agree with CEL, but the guard lives in the shared tree, where a rendering correct on one engine and wrong on two is not a rendering the translator may choose", "message": "string() over a boolean column is not supported" },
      { "action": "hier-list-id", "reason": "Same missing list() constructor as the ent module: the two share a byte-identical vendored translator, so a plan operand neither can build a path from fails closed in both", "message": "unsupported operator: list" }
    ],
    "spring-data": [
      { "action": "cast-string-double", "reason": "The Criteria API has no cast expression: a predicate compares a path against a literal or another path, and the reference's leaf resolver has no form that applies string() to the path first", "message": "Unexpected string() expression in leaf operand of eq" },
      { "action": "cast-string-bool", "reason": "Same missing cast as cast-string-double, and the one where lowering it would be wrong rather than merely absent: the dialects Spring Data JPA targets disagree on a boolean's text rendering — H2 and PostgreSQL say 'true', MySQL says '1'", "message": "Unexpected string() expression in leaf operand of eq" },
      { "action": "id-concat", "reason": "The comparison is a_optional_string against a concatenation of a constant with the primary key. The reference lowers CEL's `+` as arithmetic, which requires numeric operands; CEL's string overload has no arithmetic meaning and is refused rather than rendered as a numeric addition", "message": "Arithmetic comparison requires numeric operands, got String" },
      { "action": "concat-f2f", "reason": "The same arithmetic-only lowering of `+`, reached with both operands columns instead of one being a constant. The reference refuses every string `+`, so it needs no type information to reject the two-column form", "message": "Arithmetic comparison requires numeric operands, got String" },
      { "action": "cr-div-then-add", "reason": "CEL carries a NaN or signed infinity through the arithmetic composed on top of a division, and SQL has no value that does — the NULLIF divide-by-zero guard turns the whole sum into NULL. The reference's non-finite rewrite only reaches a division that IS the comparison operand, so a division nested inside further arithmetic fails closed rather than emitting the under-granting filter", "message": "arithmetic composed on a division whose denominator may be zero is not supported: CEL carries the resulting NaN or infinity through the surrounding arithmetic and SQL has no value that does" },
      { "action": "cr-div-then-add-ne", "reason": "Same nested-division limitation as cr-div-then-add. This is the arm that proves it matters: `NaN != 2.0` is TRUE and the PDP allows the zero row, while `NULL + 1 <> 2` is UNKNOWN and drops it", "message": "arithmetic composed on a division whose denominator may be zero is not supported: CEL carries the resulting NaN or infinity through the surrounding arithmetic and SQL has no value that does" }
    ],
    "prisma": [
      { "action": "like-percent", "reason": "Prisma emits LIKE without an ESCAPE clause and does not escape wildcards, so a needle containing % or _ cannot be matched literally; the adapter throws instead of filtering wrong", "message": "Cannot translate startsWith with a needle containing LIKE metacharacters (%, _ or \\): Prisma does not escape wildcards in string filters, and \\ is the default LIKE escape character on PostgreSQL and MySQL" },
      { "action": "like-underscore", "reason": "Same LIKE-wildcard limitation as like-percent", "message": "Cannot translate contains with a needle containing LIKE metacharacters (%, _ or \\): Prisma does not escape wildcards in string filters, and \\ is the default LIKE escape character on PostgreSQL and MySQL" },
      { "action": "like-backslash", "reason": "A backslash is a LIKE metacharacter on some providers and not others, so the same needle cannot mean the same thing on all of them: SQLite's LIKE has no escape character and matches a backslash literally, while PostgreSQL and MySQL treat it as the DEFAULT escape character with no ESCAPE clause present. endsWith('\\\\') is then a pattern ending in an escape character, which PostgreSQL rejects outright (SQLSTATE 22025), and a backslash anywhere inside a needle silently escapes the next character so contains('a\\\\b') would match 'ab' — a row the PDP denies. Prisma emits no ESCAPE clause and cannot escape the backslash, so the adapter throws instead of filtering differently per provider", "message": "Cannot translate endsWith with a needle containing LIKE metacharacters (%, _ or \\): Prisma does not escape wildcards in string filters, and \\ is the default LIKE escape character on PostgreSQL and MySQL" },
      { "action": "p-startswith-concat", "reason": "The folded needle is '100%', hitting the same LIKE-wildcard limitation as like-percent", "message": "Cannot translate startsWith with a needle containing LIKE metacharacters (%, _ or \\): Prisma does not escape wildcards in string filters, and \\ is the default LIKE escape character on PostgreSQL and MySQL" },
      { "action": "f2f-contains", "reason": "A column-valued needle would need per-row LIKE escaping, which Prisma filters cannot express; the adapter throws", "message": "Cannot translate contains between two columns: Prisma cannot escape LIKE wildcards held in a column" },
      { "action": "f2f-startswith", "reason": "Same column-needle LIKE limitation as f2f-contains", "message": "Cannot translate startsWith between two columns: Prisma cannot escape LIKE wildcards held in a column" },
      { "action": "f2f-endswith", "reason": "Same column-needle LIKE limitation as f2f-contains", "message": "Cannot translate endsWith between two columns: Prisma cannot escape LIKE wildcards held in a column" },
      { "action": "lambda-field-to-field", "reason": "Prisma field references only work between fields of the same model; comparing an element column with an outer column is cross-model, so the adapter throws", "message": "Cannot compare a collection element column with an outer column (t.name vs request.resource.attr.aString): Prisma field references only work between fields of the same model" },
      { "action": "p-lambda-f2f-like", "reason": "Cross-model comparison plus the column-needle LIKE limitation", "message": "Cannot translate contains between two columns: Prisma cannot escape LIKE wildcards held in a column" },
      { "action": "p-deep-nest", "reason": "Two independent limitations sit in this predicate, and the adapter reports the one its walk reaches first: startsWith(aString, '100%') in the left branch hits the same LIKE-metacharacter limitation as like-percent, before the right branch's cross-model element-vs-outer column comparison (t.name == R.attr.aString) — which lambda-field-to-field covers on its own — is ever visited", "message": "Cannot translate startsWith with a needle containing LIKE metacharacters (%, _ or \\): Prisma does not escape wildcards in string filters, and \\ is the default LIKE escape character on PostgreSQL and MySQL" },
      { "action": "arith-both", "reason": "Arithmetic on both sides of a comparison cannot be solved to a plain column filter; Prisma filters cannot express column arithmetic", "message": "Arithmetic on both sides of a comparison is not supported: the expression cannot be solved to a plain column filter" },
      { "action": "cr-div-zero", "reason": "Division by a column cannot be solved to a plain column filter", "message": "div operator requires exactly one field reference and one value, or two values" },
      { "action": "cr-div-zero-ne", "reason": "Same column-division limitation as cr-div-zero, reached through an inequality", "message": "div operator requires exactly one field reference and one value, or two values" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same column-division limitation as cr-div-zero, reached through a negated equality", "message": "div operator requires exactly one field reference and one value, or two values" },
      { "action": "cr-div-neg-zero", "reason": "Prisma filters have no column arithmetic, so the division must be solved algebraically into a plain column predicate — and a zero denominator has no reciprocal to solve with; the adapter throws rather than guessing the sign of the resulting infinity", "message": "Division by a constant zero is not supported" },
      { "action": "cr-div-other-column", "reason": "A denominator that is a second column leaves the comparison with two field references and no constant to solve against, which a Prisma where-filter cannot express", "message": "div operator requires exactly one field reference and one value, or two values" },
      { "action": "cr-div-then-add", "reason": "Arithmetic composed on a column division is doubly inexpressible: Prisma has no column arithmetic, and the algebraic solve has no reciprocal for a denominator that may be zero", "message": "Unsupported operator: div" },
      { "action": "cr-div-then-add-ne", "reason": "Same composed column-division limitation as cr-div-then-add, reached through an inequality", "message": "Unsupported operator: div" },
      { "action": "w1-all-chain", "reason": "every() applies to a single relation; a multi-hop chain would need the universal to be evaluated against the flattened tail while still requiring the intermediate hop, which nested every/some cannot express — the adapter throws", "message": "all() over a multi-hop relation chain is not supported" },
      { "action": "w1-size-nonneg-chain", "reason": "Prisma where-filters express only empty/non-empty via none/some, so a >= 0 relation-count threshold — which is TRUE exactly when the to-one parent exists — has no equivalent; same limitation as size-threshold", "message": "Unsupported size comparison: size(...) ge 0" },
      { "action": "size-threshold", "reason": "Prisma where-filters cannot express relation-count thresholds (only empty/non-empty via none/some)", "message": "Unsupported size comparison: size(...) gt 1" },
      { "action": "size-filter-count", "reason": "The count is taken over filter(tags, ...) rather than a named relation, and a Prisma where-filter can only count a relation it can name; the underlying relation-count limitation is size-threshold's, but this operand shape is rejected one step earlier", "message": "size operator requires a named collection operand" },
      { "action": "string-size", "reason": "Prisma has no string-length filter", "message": "Unsupported size comparison: size(...) gt 4" },
      { "action": "p-size-nested", "reason": "Same relation-count limitation as size-threshold", "message": "Unsupported size comparison: size(...) eq 1" },
      { "action": "cr-size-frac-ge", "reason": "Same relation-count limitation as size-threshold", "message": "Unsupported size comparison: size(...) ge 1.5" },
      { "action": "w1-size-frac-chain", "reason": "Same relation-count limitation as w1-size-nonneg-chain, reached through a fractional threshold: >= 1.5 rounds up to >= 2, and none/some express only emptiness", "message": "Unsupported size comparison: size(...) ge 1.5" },
      { "action": "w1-size-frac-le-chain", "reason": "Same relation-count limitation as w1-size-frac-chain under the opposite ordering: <= 1.5 rounds down to <= 1, still an arbitrary count threshold none/some cannot express", "message": "Unsupported size comparison: size(...) le 1.5" },
      { "action": "w2-outer-relation", "reason": "Embeds a size(...) == 1 count threshold; same limitation as size-threshold", "message": "Unsupported size comparison: size(...) eq 1" },
      { "action": "exists-one-multi", "reason": "exists_one requires counting matching elements, which Prisma where-filters cannot express; the adapter throws instead of silently degrading to exists", "message": "exists_one requires counting matching elements, which Prisma where-filters cannot express" },
      { "action": "n-not-exists-one-null", "reason": "Same exists_one counting limitation", "message": "exists_one requires counting matching elements, which Prisma where-filters cannot express" },
      { "action": "arith-add-eq-frac", "reason": "Prisma filters cannot express column arithmetic, and algebraically solving fractional IEEE-754 addition is not reversible for every row", "message": "Fractional addition equality cannot be translated safely: solving IEEE-754 addition into a plain Prisma column comparison is not reversible" },
      { "action": "arith-add-ne-frac", "reason": "Same fractional column-arithmetic limitation as arith-add-eq-frac", "message": "Fractional addition equality cannot be translated safely: solving IEEE-754 addition into a plain Prisma column comparison is not reversible" },
      { "action": "arith-add-eq-frac-exact", "reason": "An exact corpus witness does not make the general column-addition predicate expressible; Prisma has no column arithmetic", "message": "Fractional addition equality cannot be translated safely: solving IEEE-754 addition into a plain Prisma column comparison is not reversible" },
      { "action": "size-huge-gt", "reason": "Prisma has no string-length filter, so a giant size threshold remains inexpressible", "message": "Unsupported size comparison: size(...) gt 4294967296" },
      { "action": "size-huge-lt", "reason": "Same string-length limitation as size-huge-gt", "message": "Unsupported size comparison: size(...) lt 4294967296" },
      { "action": "hier-meta-like", "reason": "Hierarchy descendant prefixes require literal LIKE matching, but Prisma string filters cannot escape % or _; the adapter throws instead of treating them as wildcards", "message": "Cannot translate hierarchy prefix matching with LIKE metacharacters (%, _, \\ or [): Prisma emits LIKE without an ESCAPE clause, \\ is the default escape character on PostgreSQL and MySQL, and [ opens a character class on SQL Server even with one" },
      { "action": "hier-overlaps-meta", "reason": "The inclusive overlap union ends in a descendant prefix LIKE on '50%:a_b:'; Prisma emits LIKE without an ESCAPE clause, so % and _ in that prefix stay wildcards and would wrongly admit 50x:a_b:y and 50%:aXb:y — the adapter throws instead", "message": "Cannot translate hierarchy prefix matching with LIKE metacharacters (%, _, \\ or [): Prisma emits LIKE without an ESCAPE clause, \\ is the default escape character on PostgreSQL and MySQL, and [ opens a character class on SQL Server even with one" },
      { "action": "hier-bracket", "reason": "Hierarchy prefixes containing [ cannot be matched literally: Prisma emits LIKE without an ESCAPE clause, and [ opens a character class on SQL Server even when one is declared", "message": "Cannot translate hierarchy prefix matching with LIKE metacharacters (%, _, \\ or [): Prisma emits LIKE without an ESCAPE clause, \\ is the default escape character on PostgreSQL and MySQL, and [ opens a character class on SQL Server even with one" },
      { "action": "in-var-var", "reason": "Comparing a related element column with an outer scalar column is cross-model, while Prisma field references are same-model only", "message": "Membership between two database attributes is not supported: Prisma cannot compare a related element column with an outer scalar column" },
      { "action": "in-var-var-neg", "reason": "Same cross-model correlated comparison limitation as in-var-var, under negation", "message": "Membership between two database attributes is not supported: Prisma cannot compare a related element column with an outer scalar column" },
      { "action": "ts-window", "reason": "The planner emits a sub-millisecond now() literal that Prisma DateTime cannot represent without changing the CEL instant", "message": "Timestamp value exceeds millisecond precision" },
      { "action": "ts-vf", "reason": "Same sub-millisecond now() limitation in value-first form", "message": "Timestamp value exceeds millisecond precision" },
      { "action": "cast-string-double", "reason": "Prisma filters have no cast: a where clause compares a column against a value, and there is no form that applies string() to the column first. The rendering would also have to be provider-specific — CAST(x AS TEXT) formats a double differently across the providers one Prisma client targets", "message": "Unsupported operator: string" },
      { "action": "cast-string-bool", "reason": "Same missing cast as cast-string-double, and the one where lowering it would be wrong rather than merely absent: SQLite and MySQL store a boolean as 1/0, so CAST(a_bool AS TEXT) never equals CEL's \"true\" and the filter would return nothing", "message": "Unsupported operator: string" },
      { "action": "id-concat", "reason": "Both operands carry a column: a_optional_string is compared against a concatenation of a constant with the primary key. Prisma's arithmetic solving inverts the constant off the VALUE side to reach a plain column filter, which requires the other side to be a value; a concatenation involving a second column has no Prisma filter form", "message": "add operator with field references requires a value on the other side of the comparison" },
      { "action": "concat-f2f", "reason": "The same missing form reached from the other direction: here BOTH operands of the `+` are columns, so there is no constant to invert off either side and nothing to solve the comparison back into a plain column filter", "message": "add operator requires exactly one field reference and one value, or two values" }
    ],
    "drizzle": [
      { "action": "ts-window", "reason": "The planner emits a sub-millisecond now() literal that the documented Drizzle timestamp contract cannot represent without changing the CEL instant", "message": "Timestamp value exceeds millisecond precision" },
      { "action": "ts-vf", "reason": "Same sub-millisecond now() limitation in value-first form", "message": "Timestamp value exceeds millisecond precision" },
      { "action": "cast-string-bool", "reason": "CAST(... AS TEXT) reproduces CEL's string() for numeric and text columns but not for a boolean: SQLite and MySQL have no boolean type and store 1/0, so the cast renders \"1\" where CEL and PostgreSQL render \"true\". The adapter does not know its dialect by design, so no single rendering is right on every store it supports — it refused nothing before this and returned zero rows on SQLite against a 14-row oracle", "message": "CAST(... AS TEXT) reproduces CEL's string() only for numeric and text columns" },
      { "action": "id-concat", "reason": "CEL's + over strings has no dialect-independent SQL spelling: || concatenates on SQLite and PostgreSQL but is logical OR on MySQL, which spells it CONCAT(). The numeric + the adapter emits for arithmetic is silently wrong rather than a syntax error — SQLite and MySQL coerce 'prefix:' to 0, so the comparison matched nothing", "message": "Cannot translate string concatenation" },
      { "action": "id-concat-vf", "reason": "Same missing dialect-independent concatenation as id-concat, in value-first form", "message": "Cannot translate string concatenation" },
      { "action": "concat-f2f", "reason": "Same missing dialect-independent concatenation, between two columns rather than against a constant. This adapter refuses every string `+`, so the two-column case needs no extra type information to be rejected", "message": "Cannot translate string concatenation" },
      { "action": "hier-list-id", "reason": "The descendant path is CONSTRUCTED by list() from a constant segment and the primary key rather than read from a column, and the adapter's hierarchy support resolves a segmented path only from a mapped column or a plan constant", "message": "Segmented hierarchy expressions are not supported by the Drizzle adapter" }
    ],
    "sqlalchemy": [
      { "action": "cast-string-bool", "reason": "CAST(... AS VARCHAR) reproduces CEL's string() for numeric and text columns but not for a boolean: SQLite and MySQL have no boolean type and store 1/0, so the cast renders '1' where CEL and PostgreSQL render 'true'. One adapter serves every dialect SQLAlchemy does, so it refuses the shape rather than emit a filter that returns every matching row on one and none on another", "message": "'string()' over a boolean column cannot be lowered to SQL CAST" },
      { "action": "hier-list-id", "reason": "The descendant path is CONSTRUCTED by list() from a constant segment and the primary key. `list` has no entry in the operator table — it has never reached the wire from this corpus — so the walk refuses it before the hierarchy operators around it are considered", "message": "Unrecognised operator: list" },
      { "action": "ts-window", "reason": "The planner emits a nanosecond-precision now() literal that Python/SQLAlchemy DateTime cannot represent without changing the CEL instant", "message": "Timestamp literal precision exceeds the exact microsecond range" },
      { "action": "ts-vf", "reason": "Same nanosecond-precision now() limitation in value-first form", "message": "Timestamp literal precision exceeds the exact microsecond range" },
      { "action": "cr-div-neg-zero", "reason": "IEEE keeps the sign of a zero, but Cerbos's HTTP transport renders the double -0.0 as `-0` and Python's json.loads returns the INTEGER 0 for it, discarding the sign bit — so the adapter cannot tell +Infinity from -Infinity and rejects a constant zero denominator instead of guessing. The gRPC client preserves the sign (json.loads('-0.0') is -0.0), so this is a transport limitation rather than a SQL one", "message": "division by a constant zero whose sign is indeterminate: the HTTP transport renders -0.0 as `-0`, which JSON decodes to the integer 0, so the adapter cannot tell +Infinity from -Infinity. Use the gRPC client, which preserves the sign bit, or avoid a literal zero denominator" },
      { "action": "nan-ord-inf", "reason": "Same indeterminate-signed-zero rejection as cr-div-neg-zero: the 1.0/0.0 and -1.0/0.0 branches also arrive with an integer-zero denominator over HTTP, and the adapter cannot distinguish that from -0.0", "message": "division by a constant zero whose sign is indeterminate: the HTTP transport renders -0.0 as `-0`, which JSON decodes to the integer 0, so the adapter cannot tell +Infinity from -Infinity. Use the gRPC client, which preserves the sign bit, or avoid a literal zero denominator" }
    ],
    "mongoose": [
      { "action": "exists-one-multi", "reason": "exactly-one collection cardinality is not expressible by the current match-filter translator", "message": "exists_one requires exact match cardinality and is unsupported" },
      { "action": "n-not-exists-one-null", "reason": "The negation over a collection macro whose elements are nullable is rejected first, for the same three-valued reason as not-exists: a $nor would turn a CEL element error into a match. The exists_one cardinality of exists-one-multi is inexpressible underneath it either way", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "null-value-pv-not-exists", "reason": "The value-list fold produces a collection macro under a negation, which Mongoose refuses before it ever reaches the comparison leaves: $nor over an exists would turn a CEL element error into a match. The explicit-null convention this action probes is expressible in Mongoose (its four siblings are oracle-compared) — the negated macro above it is not", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "lambda-field-to-field", "reason": "MongoDB $expr cannot be safely scoped inside the adapter's $elemMatch predicates", "message": "eq aggregation expressions inside collection predicates are unsupported" },
      { "action": "outer-attr-depth2", "reason": "outer resource references inside nested element predicates require aggregation pipeline scoping", "message": "Outer reference request.resource.attr.aBool inside a collection predicate is unsupported" },
      { "action": "p-arith-in-lambda", "reason": "MongoDB rejects $expr below the top level of a query ('$expr can only be applied to the top-level document'), and field arithmetic exists only as an aggregation expression inside $expr; the per-element scope this needs comes from $elemMatch, so no find filter is both element-scoped and expression-valued. It would take an aggregation pipeline ($map/$filter plus $anyElementTrue), which a Mongoose Query filter is not", "message": "gt aggregation expressions inside collection predicates are unsupported" },
      { "action": "p-lambda-inner-f2f", "reason": "Comparing two fields of the same array element is only expressible as an aggregation expression, and MongoDB rejects $expr below the top level ('$expr can only be applied to the top-level document'), so it cannot be placed inside the $elemMatch that establishes the element scope", "message": "eq aggregation expressions inside collection predicates are unsupported" },
      { "action": "p-lambda-f2f-like", "reason": "Using an outer document field as the needle of a string search over an array element needs an aggregation expression over a $-anchored path, and MongoDB rejects $expr below the top level ('$expr can only be applied to the top-level document'), so it cannot sit inside the $elemMatch that scopes the element", "message": "contains aggregation expressions inside collection predicates are unsupported" },
      { "action": "p-size-nested", "reason": "nested collection size requires aggregation pipeline scoping", "message": "eq aggregation expressions inside collection predicates are unsupported" },
      { "action": "p-ternary-in-exists", "reason": "$cond exists only as an aggregation expression, and MongoDB rejects $expr below the top level ('$expr can only be applied to the top-level document'), so a conditional cannot be evaluated inside the $elemMatch that establishes the element scope", "message": "eq aggregation expressions inside collection predicates are unsupported" },
      { "action": "p-ternary-under-all", "reason": "Same top-level-only $expr restriction as p-ternary-in-exists, reached through all(): the negated $elemMatch that expresses all() still cannot contain the $cond aggregation expression the conditional needs", "message": "ne aggregation expressions inside collection predicates are unsupported" },
      { "action": "p-ternary-vs-ternary", "reason": "Mongoose's $expr caster drops the schema when it recurses into a non-literal right-hand operand (cast$expr.js castComparison calls _castExpression(val[1]) with no schema argument), so a comparison whose right side is another $cond containing a path/literal comparison throws TypeError: Cannot read properties of undefined (reading 'path') while the query is built. MongoDB itself evaluates $cond against $cond correctly, so a Mongoose fix would change this answer", "message": "Mongoose cannot cast comparisons between two conditional expressions" },
      { "action": "p-deep-nest", "reason": "The tags.exists lambda compares an element's name with the root document's aString. $elemMatch rebases every field path onto the array element, and the only route back to a root field is a $-anchored path inside $expr, which MongoDB rejects below the top level ('$expr can only be applied to the top-level document')", "message": "eq aggregation expressions inside collection predicates are unsupported" },
      { "action": "size-filter-count", "reason": "size(filter(...)) requires an aggregation pipeline expression", "message": "Unsupported operator inside aggregation expression: filter" },
      { "action": "w2-outer-relation", "reason": "The categories.exists lambda reads the root document's tags collection from inside the element scope. $elemMatch rebases every field path onto the array element, and the only route back to a root field is a $-anchored path inside $expr, which MongoDB rejects below the top level ('$expr can only be applied to the top-level document')", "message": "eq aggregation expressions inside collection predicates are unsupported" },
      { "action": "in-var-var", "reason": "variable-in-variable membership requires correlated aggregation scoping", "message": "in supports only field-in-value-list or value-in-mapped-collection shapes" },
      { "action": "in-var-var-neg", "reason": "same variable-in-variable limitation under negation", "message": "in supports only field-in-value-list or value-in-mapped-collection shapes" },
      { "action": "cr-div-zero", "reason": "a row-dependent zero denominator aborts a Mongo query instead of denying that row", "message": "div operator requires a non-zero constant denominator" },
      { "action": "cr-div-zero-ne", "reason": "Same zero-denominator abort as cr-div-zero, reached through an inequality", "message": "div operator requires a non-zero constant denominator" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same zero-denominator abort as cr-div-zero, reached through a negated equality", "message": "div operator requires a non-zero constant denominator" },
      { "action": "cr-div-neg-zero", "reason": "$divide aborts the whole query on a zero denominator instead of producing CEL's signed infinity for that document, so the adapter requires a non-zero constant denominator and rejects a literal zero", "message": "div operator requires a non-zero constant denominator" },
      { "action": "cr-div-other-column", "reason": "A denominator that is a second field can be zero for some documents, and $divide aborts the query rather than denying just those; the adapter requires a non-zero constant denominator", "message": "div operator requires a non-zero constant denominator" },
      { "action": "cr-div-then-add", "reason": "Same zero-denominator abort as cr-div-zero, reached through arithmetic composed on the division", "message": "div operator requires a non-zero constant denominator" },
      { "action": "cr-div-then-add-ne", "reason": "Same zero-denominator abort as cr-div-then-add, reached through an inequality", "message": "div operator requires a non-zero constant denominator" },
      { "action": "w1-not-exists-chain", "reason": "Negating a collection macro over nullable element members would turn CEL errors into matches, the same three-valued limitation as not-exists — and the chain adds an absent to-one parent, which a Mongo filter has no UNKNOWN to represent", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "w1-ternary-chain-cond", "reason": "A bare ternary is planned as $expr/$cond, and an aggregation expression has no membership form here: MongoDB's $in aggregation operator raises a runtime error when its array operand is missing, which is exactly the absent-to-one-parent case this action probes, so lowering it would fail the whole query rather than deny the row", "message": "Unsupported operator inside aggregation expression: in" },
      { "action": "nan-ord-ternary", "reason": "MongoDB division by zero aborts rather than producing CEL non-finite values", "message": "div operator requires a non-zero constant denominator" },
      { "action": "nan-ord-ternary-vf", "reason": "same non-finite arithmetic limitation in value-first form", "message": "div operator requires a non-zero constant denominator" },
      { "action": "nan-ord-le", "reason": "Both sides are conditionals, so the Mongoose $expr caster fails to build the comparison for the same reason as p-ternary-vs-ternary; independently, the 0.0/0.0 branch is rejected because $divide by zero fails at query-optimization time ('can't $divide by zero'), aborting the whole query even though no document takes that branch", "message": "Mongoose cannot cast comparisons between two conditional expressions" },
      { "action": "nan-ord-inf", "reason": "$divide by a zero denominator fails when MongoDB optimizes the query ('can't $divide by zero'), aborting it before any document is read rather than yielding CEL's +Inf/-Inf; constant folding means even a $cond arm no document takes still aborts, so the adapter rejects the literal 1.0/0.0 and -1.0/0.0 branches", "message": "div operator requires a non-zero constant denominator" },
      { "action": "not-exists", "reason": "negating a nullable collection macro would turn CEL errors into matches", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "macro-depth3-not-exists", "reason": "same nullable macro-negation limitation across three collection levels", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "n-not-all-null", "reason": "negated all over nullable element members needs explicit three-valued logic", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "n-not-all-absorb", "reason": "same nullable all-negation limitation with an absorbing false witness", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "p-not-exists-empty", "reason": "negated exists over nullable element members needs explicit three-valued logic", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "p-not-ternary-null", "reason": "negating a ternary over a nullable field cannot preserve CEL error semantics", "message": "not over nullable fields or collection macros cannot preserve Cerbos error semantics" },
      { "action": "ts-window", "reason": "the planner emits a nanosecond-precision now() literal that BSON Date cannot represent without changing the CEL instant", "message": "timestamp value must be a millisecond-exact RFC 3339 instant in the CEL range" },
      { "action": "ts-vf", "reason": "same nanosecond-precision now() limitation in value-first form", "message": "timestamp value must be a millisecond-exact RFC 3339 instant in the CEL range" },
      { "action": "hier-list-id", "reason": "the descendant path is CONSTRUCTED by list() from a constant segment and the primary key rather than read from a field, and the adapter resolves a hierarchy operand only from a mapped field or a string constant — it has no form for a path assembled at query time", "message": "hierarchy path must be a field or string value" },
      { "action": "concat-f2f", "reason": "a CONSTANT is what tells CEL's two `+` overloads apart, and between two field paths there is none. MongoDB spells the two differently — $add accepts numeric and date types only, $concat strings — and the mapper carries no field types, so neither can be chosen. Sending $add anyway is what the server aborts the whole query on, so the shape is refused at translation instead", "message": "Cannot tell numeric addition from string concatenation in '+' between two fields" }
    ],
    "langchain-chromadb": [
      { "action": "like-percent", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator startsWith" },
      { "action": "like-underscore", "reason": "Chroma metadata filters have no substring or pattern operator, so contains has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator contains" },
      { "action": "like-backslash", "reason": "Chroma metadata filters have no suffix or pattern operator, so endsWith has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator endsWith" },
      { "action": "double-threshold", "reason": "Chroma distinguishes integer and floating-point metadata; a fractional threshold against this integer field is not faithful", "message": "ge cannot safely compare a fractional threshold unless the mapped Chroma metadata field declares numericType: \"float\"" },
      { "action": "all-on-empty", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "exists-on-empty", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "exists-one-multi", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "not-exists", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one", "message": "Cannot negate operator exists" },
      { "action": "outer-attr-depth2", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "lambda-in-principal", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "not-empty", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "optional-ne", "reason": "Chroma $ne includes records with missing metadata, unlike Cerbos missing-attribute error semantics", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "lambda-field-to-field", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "field-to-field", "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "size-threshold", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "size-filter-count", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "string-size", "reason": "Chroma metadata filters have no string-length function, and every comparison operand must be a bare field or literal, so size(field) cannot be an operand", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ternary-cmp", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ternary-expr-cond", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ternary-nested", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ternary-negated", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ternary-bare", "reason": "The bare ternary plans as a three-operand conditional at the top of the filter; Chroma's Where model only takes two-operand field-vs-literal comparisons", "message": "Expected exactly two operands" },
      { "action": "ternary-value-first", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ternary-null-cond", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "f2f-contains", "reason": "Chroma has no field-to-field operand and no substring or pattern operator, so a column-valued needle is doubly inexpressible", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "f2f-startswith", "reason": "Chroma has no field-to-field operand and no substring or pattern operator, so a column-valued needle is doubly inexpressible", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "f2f-endswith", "reason": "Chroma has no field-to-field operand and no substring or pattern operator, so a column-valued needle is doubly inexpressible", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "arith-add", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-vf", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-sub", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-mult-neg", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-div", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-div-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-both", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-ternary-in-exists", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-arith-in-lambda", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-lambda-inner-f2f", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-lambda-f2f-like", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-size-nested", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-ternary-of-ternaries", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-ternary-vs-ternary", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-ternary-under-all", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-hasintersection-map", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-deep-nest", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-startswith-concat", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator startsWith" },
      { "action": "p-not-exists-empty", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one", "message": "Cannot negate operator exists" },
      { "action": "p-not-ternary-null", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "p-double-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "n-not-exists-one-null", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one", "message": "Cannot negate operator exists_one" },
      { "action": "n-all-mixed-null", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "n-not-all-null", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one", "message": "Cannot negate operator all" },
      { "action": "n-not-all-absorb", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one", "message": "Cannot negate operator all" },
      { "action": "cr-contains", "reason": "Chroma metadata filters have no substring or pattern operator, so contains has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator contains" },
      { "action": "cr-startswith", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator startsWith" },
      { "action": "cr-endswith", "reason": "Chroma metadata filters have no suffix or pattern operator, so endsWith has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator endsWith" },
      { "action": "cr-startswith-concat", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator startsWith" },
      { "action": "cr-div-zero", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cr-div-zero-ne", "reason": "Same flat-scalar-metadata limitation as cr-div-zero: the arithmetic operand cannot be translated at all, whatever the enclosing comparison", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same flat-scalar-metadata limitation as cr-div-zero under a negated equality", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cr-div-neg-zero", "reason": "Same flat-scalar-metadata limitation as cr-div-zero: Chroma compares a stored field against a literal only, so a division operand cannot be translated whatever its denominator", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cr-div-other-column", "reason": "Same flat-scalar-metadata limitation as cr-div-zero, with a second metadata key as the denominator — Chroma has neither arithmetic nor a field-to-field operand", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cr-div-then-add", "reason": "Same flat-scalar-metadata limitation as cr-div-zero, with addition composed on the division", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cr-div-then-add-ne", "reason": "Same flat-scalar-metadata limitation as cr-div-then-add, reached through an inequality", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-all-chain", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, so the chained collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-not-exists-chain", "reason": "Chroma has no negation of a collection predicate, and no relation model to reach the chained collection with in the first place", "message": "Cannot negate operator exists" },
      { "action": "w1-size-zero-chain", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-size-nonneg-chain", "reason": "Same missing relation model and count function as w1-size-zero-chain", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cr-size-frac-ge", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-exists-chain", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-size-chain", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-in-chain", "reason": "Chroma's $in tests a metadata field against a literal list; it cannot test whether a literal is a member of a list-valued metadata field, and Chroma metadata values are scalars anyway", "message": "ChromaDB filters cannot test whether a literal is contained in a metadata field" },
      { "action": "w1-not-in-chain", "reason": "Same literal-in-field limitation as w1-in-chain, reached under a negation: the adapter rejects the membership direction before the negation is even considered", "message": "ChromaDB filters cannot test whether a literal is contained in a metadata field" },
      { "action": "w1-not-hasint-chain", "reason": "Chroma has no operator that intersects a list-valued metadata field with a literal list, so hasIntersection has no Where equivalent to negate; the adapter refuses to invert it", "message": "Cannot negate operator hasIntersection" },
      { "action": "w1-not-size-chain", "reason": "Chroma comparison operands must be a bare field or literal, so the negated size(...) collection count is rejected as a nested expression — the same missing relation model and count function as w1-size-zero-chain", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-ternary-chain-cond", "reason": "Same three-operand conditional as ternary-bare: Chroma's Where model takes only two-operand field-vs-literal comparisons, so the ternary is rejected at the top of the filter before its chained condition is examined", "message": "Expected exactly two operands" },
      { "action": "w1-size-frac-chain", "reason": "Same missing relation model and count function as w1-size-zero-chain, reached through a fractional threshold", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w1-size-frac-le-chain", "reason": "Same missing relation model and count function as w1-size-frac-chain under the opposite ordering", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "w2-outer-relation", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "like-bracket", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it", "message": "Unsupported operator startsWith" },
      { "action": "arith-add-eq-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-add-ne-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "arith-add-eq-frac-exact", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "size-huge-gt", "reason": "Chroma metadata filters have no string-length function, and every comparison operand must be a bare field or literal, so size(field) cannot be an operand", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "size-huge-lt", "reason": "Chroma metadata filters have no string-length function, and every comparison operand must be a bare field or literal, so size(field) cannot be an operand", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "nan-ord-ternary", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "nan-ord-ternary-vf", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "nan-ord-le", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "nan-ord-inf", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "macro-depth3-exists", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "macro-depth3-not-exists", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one", "message": "Cannot negate operator exists" },
      { "action": "macro-depth3-all", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "pv-exists", "reason": "Chroma has no collection-macro operator to fold the lambda into: above the planner's 10-element unroll cap the collection arrives as a literal value list with a lambda body, and $in matches a field against a list rather than evaluating a per-element predicate. Write membership as `R.attr.x in P.attr.xs`, which the planner emits as `in` + value list at every collection size and maps to a single $in", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "pv-all", "reason": "Same missing collection-macro operator as pv-exists; additionally the unrolled form is a chain of $ne, which Chroma can only express safely when the mapper declares the metadata key required, since a document missing the key matches $ne", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-ancestor-ff", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-ancestor-cf", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-descendent-ff", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-descendent-cf", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-overlaps-ff", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-overlaps-cf", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-meta-like", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-meta-in", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-overlaps-meta", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-bracket", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ts-window", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ts-vf", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ts-eq", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ts-eq-offset", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "ts-ne", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "in-null-elem-mixed", "reason": "Chroma metadata values are non-null scalars, so a membership list containing null cannot be represented and the adapter refuses to silently drop the null element", "message": "in requires a list containing only finite numbers, strings, or booleans" },
      { "action": "in-null-elem-neg", "reason": "Chroma $nin matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required", "message": "nin is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "in-null-elem-only", "reason": "The planner folds membership in [null] to an equality against null; Chroma metadata has no null value, so the comparison cannot be represented", "message": "eq requires a finite number, string, or boolean literal" },
      { "action": "in-null-elem-only-neg", "reason": "Chroma $ne matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "in-null-elem-rel", "reason": "Chroma's $in tests a metadata field against a literal list; it cannot test whether a literal is a member of a list-valued metadata field, and Chroma metadata values are scalars anyway", "message": "ChromaDB filters cannot test whether a literal is contained in a metadata field" },
      { "action": "in-null-elem-rel-neg", "reason": "Chroma's $in tests a metadata field against a literal list; it cannot test whether a literal is a member of a list-valued metadata field, and Chroma metadata values are scalars anyway", "message": "ChromaDB filters cannot test whether a literal is contained in a metadata field" },
      { "action": "in-null-elem-hasint", "reason": "Chroma metadata values are scalars with no list-intersection operator, so hasIntersection has no Where equivalent", "message": "Unsupported operator hasIntersection" },
      { "action": "in-var-var", "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "in-var-var-neg", "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "null-eq", "reason": "Chroma $eq accepts only a finite number, string or boolean literal, and Chroma metadata has no null value, so an equality against null cannot be represented", "message": "eq requires a finite number, string, or boolean literal" },
      { "action": "null-ne", "reason": "Chroma $ne matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "vf-null-ne", "reason": "Value-first form of null-ne; the same $ne absent-key mismatch applies after the planner mirrors the operands", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "null-not-eq", "reason": "Negating the null equality routes through the same $ne absent-key mismatch as null-ne", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "null-value-ne-const", "reason": "Chroma $ne matches records whose metadata key is absent, and `owner` is stored as absent for a NULL column because Chroma metadata has no null value. The explicit-null convention this action probes therefore cannot even be represented in the store, let alone compared", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "null-value-not-eq-const", "reason": "The negated-equality spelling lowers to the same $ne absent-key mismatch as null-value-ne-const", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "null-value-not-in-const", "reason": "The negated membership lowers to $nin, which matches records whose metadata key is absent for the same reason $ne does", "message": "nin is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "null-value-f2f", "reason": "Chroma Where documents compare a metadata field against a literal only, so the field-to-field operand is rejected before the null convention on either side is reached — the same limitation as field-to-field", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "null-value-pv-not-exists", "reason": "The value-list fold puts a collection macro under a negation, and Chroma has no negation for `exists` — the macro is refused before the comparison leaves it folds into are reached, the same limitation as pv-exists reaches from the positive side", "message": "Cannot negate operator exists" },
      { "action": "rel-not-bool-hop", "reason": "Chroma lowers a negated boolean to $ne, and a document whose to-one parent is absent carries no parent.aBool key at all — a missing metadata key MATCHES $ne, which is exactly the parentless rows the PDP denies. The mapping therefore leaves every chain key optional and the adapter refuses $ne over an optional field rather than emitting the over-grant", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "rel-ne-null-hop", "reason": "Same optional-metadata $ne limitation as rel-not-bool-hop, reached through the null comparison: `!= null` over a chain key lowers to $ne, which a document missing that key satisfies. Both flavours of absence — no parent at all, and a parent whose optional column is NULL — are stored as a missing key, so there is nothing for Chroma to distinguish them with", "message": "ne is unsafe for optional Chroma metadata because missing fields match the filter" },
      { "action": "rel-contains-hop", "reason": "Chroma metadata filters expose only scalar comparison and membership operators — there is no substring operator — so contains has no faithful Where equivalent, through a chain key exactly as at the root", "message": "Unsupported operator contains" },
      { "action": "rel-startswith-hop2", "reason": "Same missing pattern-operator limitation as like-percent, reached two levels out: Chroma has no prefix operator for the flattened parent.inner.aString key", "message": "Unsupported operator startsWith" },
      { "action": "rel-hop-and-root", "reason": "The conjunction itself is expressible, but its second conjunct is a contains against the chain key, which Chroma has no substring operator for — the walk reaches that limitation before the conjunction is assembled", "message": "Unsupported operator contains" },
      { "action": "rel-hop2-or-exists", "reason": "Every Chroma comparison operand must be a bare metadata field or a literal, and the disjunction's second branch is an exists macro over a relation — a nested expression the Where model has no form for. The two-level hop in the first branch is never reached", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cs-contains", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) — there is no substring operator — so the case-sensitivity this action probes is never reached: the operator is refused first, exactly as for like-underscore", "message": "Unsupported operator contains" },
      { "action": "cs-startswith", "reason": "Same missing pattern-operator limitation as like-percent: Chroma has no prefix operator, so the comparison whose case-sensitivity this action probes is never built", "message": "Unsupported operator startsWith" },
      { "action": "cs-endswith", "reason": "Same missing pattern-operator limitation as like-backslash: Chroma has no suffix operator, so the comparison whose case-sensitivity this action probes is never built", "message": "Unsupported operator endsWith" },
      { "action": "cast-string-double", "reason": "Every Chroma comparison operand must be a bare metadata field or a literal. string() wraps the field in an expression, and Chroma's Where model has no computed operand at all — there is no cast, and no place to put one", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "cast-string-bool", "reason": "Same missing computed operand as cast-string-double", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "id-f2f", "reason": "Comparing the primary key against a metadata field is variable-to-variable: a Chroma Where clause compares one metadata key against a LITERAL, and has no form that names a second key on the value side", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "id-f2f-ne", "reason": "Same variable-to-variable limitation as id-f2f, under negation", "message": "Variable-to-variable comparisons are not supported by ChromaDB filters" },
      { "action": "id-concat", "reason": "The value side is a concatenation of a constant with the primary key — a computed operand, which the Where model has no form for. The comparison is refused before the second metadata key it names is reached", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "id-concat-vf", "reason": "Same missing computed operand as id-concat, in value-first form", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "hier-list-id", "reason": "Chroma metadata filters have no path or prefix operator, and both hierarchy(...) operands are computed — one of them from a list() built around the primary key — so neither side reduces to the bare field or literal a Where clause requires", "message": "Nested expressions are not supported by ChromaDB filters" },
      { "action": "concat-f2f", "reason": "The comparison's left operand is a concatenation of two metadata fields — a computed operand, which the Where model has no form for. Chroma never reaches the question of which `+` overload the plan means", "message": "Nested expressions are not supported by ChromaDB filters" }
    ],
    "convex": [
      { "action": "cr-div-neg-zero", "reason": "IEEE keeps the sign of a zero, and the planner does ship it (the wire operand for -0.0 is `-0`), but a query plan is JSON-encoded on its way into a Convex function and JSON.stringify(-0) is \"0\" — the sign bit is gone before the adapter sees it, so it rejects a constant zero denominator rather than guess which infinity CEL produced", "message": "division by a constant zero whose sign is indeterminate: a query plan is JSON-encoded on its way into a Convex function and JSON has no -0, so the adapter cannot tell +Infinity from -Infinity" },
      { "action": "nan-ord-inf", "reason": "Same indeterminate-signed-zero rejection as cr-div-neg-zero: the 1.0/0.0 and -1.0/0.0 branches also carry a constant zero denominator, which survives the JSON hop as an unsigned 0", "message": "division by a constant zero whose sign is indeterminate: a query plan is JSON-encoded on its way into a Convex function and JSON has no -0, so the adapter cannot tell +Infinity from -Infinity" },
      { "action": "hier-list-id", "reason": "The descendant path is CONSTRUCTED by list() from a constant segment and the primary key. `list` is not in the adapter's known-operator set at all — it has never reached the wire from this corpus — so the shape is refused during structural validation, before the hierarchy operators around it are considered", "message": "Unsupported operator: list" }
    ],
    "elasticsearch-java": [
      { "action": "all-on-empty", "reason": "Elasticsearch does not index empty nested arrays, so a positive all predicate cannot distinguish an empty collection (true) from a missing collection (CEL error)", "message": "all cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "exists-one-multi", "reason": "Elasticsearch Query DSL cannot count nested matches to express exists_one without scripts", "message": "exists_one cannot be expressed by Elasticsearch nested queries without scripts" },
      { "action": "not-exists", "reason": "Elasticsearch does not index empty nested arrays, so negated exists cannot distinguish an empty collection (true) from a missing collection (CEL error)", "message": "Negated exists cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "outer-attr-depth2", "reason": "Elasticsearch nested queries cannot preserve a resource-field correlation across two nested scopes without scripts", "message": "Unexpected lambda expression in leaf operand" },
      { "action": "lambda-field-to-field", "reason": "Elasticsearch Query DSL cannot compare a nested field with an outer resource field without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "field-to-field", "reason": "Elasticsearch Query DSL cannot compare two document fields without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "size-threshold", "reason": "Elasticsearch Query DSL cannot express arbitrary collection-count thresholds without scripts", "message": "Unsupported size comparison: size(request.resource.attr.tags) gt 1.0. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "size-filter-count", "reason": "Elasticsearch Query DSL cannot count only nested elements matching a predicate without scripts", "message": "Unsupported size() expression" },
      { "action": "string-size", "reason": "Elasticsearch Query DSL cannot compare a string's length without scripts", "message": "Unsupported size comparison: size(request.resource.attr.aString) gt 4.0. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "ternary-cmp", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "ternary-expr-cond", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "ternary-nested", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "ternary-negated", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "ternary-bare", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts", "message": "if requires exactly 2 operands, got 3" },
      { "action": "ternary-value-first", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "ternary-null-cond", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "f2f-contains", "reason": "Elasticsearch Query DSL cannot use one document field as another field's string-search needle without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "f2f-startswith", "reason": "Elasticsearch Query DSL cannot use one document field as another field's string-search needle without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "f2f-endswith", "reason": "Elasticsearch Query DSL cannot use one document field as another field's string-search needle without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "arith-add", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "arith-vf", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "arith-sub", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts", "message": "Unexpected sub expression in leaf operand" },
      { "action": "arith-mult-neg", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts", "message": "Unexpected mult expression in leaf operand" },
      { "action": "arith-div", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts", "message": "Unexpected div expression in leaf operand" },
      { "action": "arith-div-frac", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts", "message": "Unexpected div expression in leaf operand" },
      { "action": "arith-both", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "p-ternary-in-exists", "reason": "Elasticsearch Query DSL cannot evaluate a conditional value inside a nested predicate without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "p-arith-in-lambda", "reason": "Elasticsearch Query DSL cannot evaluate arithmetic inside a nested predicate without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "p-lambda-inner-f2f", "reason": "Elasticsearch Query DSL cannot compare fields within a nested element without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "p-lambda-f2f-like", "reason": "Elasticsearch Query DSL cannot use an outer document field as a nested string-search needle without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "p-size-nested", "reason": "Elasticsearch Query DSL cannot express arbitrary nested collection-count thresholds without scripts", "message": "Unexpected size expression in leaf operand" },
      { "action": "p-ternary-of-ternaries", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to nested CEL ternaries without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "p-ternary-vs-ternary", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to nested CEL ternaries without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "p-ternary-under-all", "reason": "The positive all() is rejected before its body is examined, for the same reason as all-on-empty: Elasticsearch does not index an empty nested array, so all cannot distinguish an empty collection (true) from a missing one (CEL error). The conditional inside it would need scripts too, but the outer macro fails closed first", "message": "all cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "p-deep-nest", "reason": "This predicate combines field arithmetic and nested-to-outer field comparison, neither expressible by Elasticsearch Query DSL without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "p-not-exists-empty", "reason": "Elasticsearch does not index empty nested arrays, so negated exists cannot distinguish an empty collection (true) from a missing collection (CEL error)", "message": "Negated exists cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "p-not-ternary-null", "reason": "Elasticsearch Query DSL cannot preserve CEL null/error semantics through a negated conditional value without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "p-double-frac", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic with fractional values without scripts", "message": "Unexpected mult expression in leaf operand" },
      { "action": "n-all-mixed-null", "reason": "Positive all is unsafe because Elasticsearch cannot distinguish an empty nested collection from a missing collection, in addition to the nullable-element CEL error semantics this action exercises", "message": "all cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "n-not-exists-one-null", "reason": "Elasticsearch Query DSL cannot count nested matches or preserve exists_one null/error semantics without scripts", "message": "exists_one cannot be expressed by Elasticsearch nested queries without scripts" },
      { "action": "cr-contains", "reason": "This value-first string predicate uses a document field as the search needle against a constant haystack, which Elasticsearch Query DSL cannot express without scripts", "message": "contains with a document field as the receiver argument cannot be expressed without scripts" },
      { "action": "cr-startswith", "reason": "This value-first string predicate uses a document field as the search needle against a constant haystack, which Elasticsearch Query DSL cannot express without scripts", "message": "startsWith with a document field as the receiver argument cannot be expressed without scripts" },
      { "action": "cr-endswith", "reason": "This value-first string predicate uses a document field as the search needle against a constant haystack, which Elasticsearch Query DSL cannot express without scripts", "message": "endsWith with a document field as the receiver argument cannot be expressed without scripts" },
      { "action": "cr-startswith-concat", "reason": "After constant folding, this value-first predicate still uses a document field as the search needle against a constant haystack, which requires scripts", "message": "startsWith with a document field as the receiver argument cannot be expressed without scripts" },
      { "action": "cr-div-zero", "reason": "Elasticsearch Query DSL cannot evaluate division by a document field without scripts", "message": "Unexpected div expression in leaf operand" },
      { "action": "cr-div-zero-ne", "reason": "Elasticsearch Query DSL cannot evaluate division by a document field without scripts, whatever the enclosing comparison", "message": "Unexpected div expression in leaf operand" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same scriptless-division limitation as cr-div-zero under a negated equality", "message": "Unexpected div expression in leaf operand" },
      { "action": "cr-size-frac-ge", "reason": "Elasticsearch Query DSL cannot compare collection counts against arbitrary thresholds without scripts", "message": "Unsupported size comparison: size(request.resource.attr.tags) ge 1.5. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "w2-outer-relation", "reason": "Elasticsearch Query DSL cannot correlate nested traversal with an outer relation-count predicate without scripts", "message": "Unexpected size expression in leaf operand" },
      { "action": "arith-add-eq-frac", "reason": "Elasticsearch Query DSL cannot evaluate IEEE-754 document-field addition without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "arith-add-ne-frac", "reason": "Elasticsearch Query DSL cannot evaluate IEEE-754 document-field addition without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "arith-add-eq-frac-exact", "reason": "Elasticsearch Query DSL cannot evaluate IEEE-754 document-field addition without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "size-huge-gt", "reason": "Elasticsearch Query DSL cannot compare a string's length without scripts", "message": "Unsupported size comparison: size(request.resource.attr.aString) gt 4.294967296E9. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "size-huge-lt", "reason": "Elasticsearch Query DSL cannot compare a string's length without scripts", "message": "Unsupported size comparison: size(request.resource.attr.aString) lt 4.294967296E9. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "nan-ord-ternary", "reason": "Elasticsearch Query DSL cannot reproduce CEL NaN ordering through conditional values without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "nan-ord-ternary-vf", "reason": "Elasticsearch Query DSL cannot reproduce CEL NaN ordering through conditional values without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "nan-ord-le", "reason": "Elasticsearch Query DSL cannot evaluate the non-finite arithmetic operand required by this CEL predicate without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "nan-ord-inf", "reason": "Elasticsearch Query DSL cannot evaluate the non-finite arithmetic operand required by this CEL predicate without scripts", "message": "Unexpected if expression in leaf operand" },
      { "action": "macro-depth3-exists", "reason": "Elasticsearch Query DSL cannot preserve three levels of nested collection scope with the configured mappings", "message": "Unexpected lambda expression in leaf operand" },
      { "action": "macro-depth3-not-exists", "reason": "The negated exists is rejected before the nesting depth matters, for the same reason as not-exists: a bool must_not over a nested collection matches a document with no nested array at all, and Elasticsearch cannot tell that from one whose array holds no match. Three levels of nested scope are inexpressible underneath it either way", "message": "Negated exists cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "macro-depth3-all", "reason": "Elasticsearch Query DSL cannot preserve three levels of nested collection scope and CEL all semantics", "message": "Unexpected lambda expression in leaf operand" },
      { "action": "hier-ancestor-ff", "reason": "Elasticsearch Query DSL has no hierarchy relation: a strict-ancestor test only lowers by enumerating the constant path's proper prefixes into a terms query. ancestorOf has no case in the adapter's operator table, so it falls through to the generic leaf path, where resolveLeafOperand accepts a variable, a value or a timestamp() wrapper and rejects the plan's hierarchy(...) wrapper ('Unexpected hierarchy expression in leaf operand'). Adding that lowering later would change this answer", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-ancestor-cf", "reason": "Elasticsearch Query DSL has no hierarchy relation: with the constant on the ancestor side this is a strict-descendant test, which only lowers to a prefix query on the constant plus its delimiter over a keyword-mapped field. It dies where hier-ancestor-ff does — hierarchy(...) is not a leaf operand the adapter can resolve", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-descendent-ff", "reason": "Elasticsearch Query DSL has no hierarchy relation: a strict-descendant test only lowers to a prefix query on the constant plus its delimiter over a keyword-mapped field. descendentOf has no case in the adapter's operator table, so the plan's hierarchy(...) wrappers reach the generic leaf path and are rejected there ('Unexpected hierarchy expression in leaf operand')", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-descendent-cf", "reason": "Elasticsearch Query DSL has no hierarchy relation: with the constant on the descendant side this is a strict-ancestor test, which only lowers by enumerating the constant path's proper prefixes into a terms query. It dies where hier-descendent-ff does — hierarchy(...) is not a leaf operand the adapter can resolve", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-overlaps-ff", "reason": "Elasticsearch Query DSL has no hierarchy relation, and overlaps is the union of the ancestor, equal and descendant cases: it only lowers to a bool.should over a terms prefix enumeration, a term and a prefix query. overlaps has no case in the adapter's operator table, so the plan's hierarchy(...) wrappers reach the generic leaf path and are rejected there ('Unexpected hierarchy expression in leaf operand')", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-overlaps-cf", "reason": "overlaps is symmetric, so the constant-first spelling needs the same bool.should over a terms prefix enumeration, a term and a prefix query as hier-overlaps-ff, and dies at the same point: hierarchy(...) is not a leaf operand the adapter can resolve", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-meta-like", "reason": "Blocked by the same unresolvable hierarchy(...) leaf operand as hier-descendent-ff. The % and _ path segments are not what stops it: a prefix query over a keyword field is term-level and matches them literally, so unlike the SQL adapters this shape would need no metacharacter escaping", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-meta-in", "reason": "Blocked by the same unresolvable hierarchy(...) leaf operand as hier-ancestor-ff. The % and _ path segments are not what stops it: a terms query matches path segments literally, so unlike the SQL adapters this shape would need no metacharacter escaping", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-overlaps-meta", "reason": "Blocked by the same unresolvable hierarchy(...) leaf operand as hier-overlaps-ff, so the overlap union never reaches a prefix query. The % and _ path segments are not what stops it: term-level Elasticsearch queries match them literally", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "hier-bracket", "reason": "Blocked by the same unresolvable hierarchy(...) leaf operand as hier-descendent-ff, here with a custom ':' delimiter. The [ and ] path segments are not what stops it: a prefix query over a keyword field matches them literally", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "cast-string-double", "reason": "The Query DSL has no computed operand: a term or range query compares a FIELD against a literal, and there is no place to apply a cast to the field first. Painless runtime scripts could express it, which is the enhancement #226 tracks", "message": "Unexpected string expression in leaf operand" },
      { "action": "cast-string-bool", "reason": "Same missing computed operand as cast-string-double", "message": "Unexpected string expression in leaf operand" },
      { "action": "id-f2f", "reason": "Comparing the primary key against another document field is field-to-field, which the Query DSL cannot express without a script", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "id-f2f-ne", "reason": "Same field-to-field limitation as id-f2f, under negation", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "id-concat", "reason": "The value side is a concatenation of a constant with the primary key — a computed operand the Query DSL has no form for. The comparison is refused before the second field it names is reached", "message": "Unexpected add expression in leaf operand" },
      { "action": "id-concat-vf", "reason": "Same missing computed operand as id-concat, in value-first form", "message": "Unexpected add expression in leaf operand" },
      { "action": "concat-f2f", "reason": "Same missing computed operand as id-concat, with both `+` operands document fields rather than one being the primary key. Painless runtime scripts could express it, which is the enhancement #226 tracks", "message": "Unexpected add expression in leaf operand" },
      { "action": "hier-list-id", "reason": "Blocked by the same unresolvable hierarchy(...) leaf operand as hier-descendent-ff. Here the operand is not merely computed but CONSTRUCTED by list() around the primary key, so there is no field for the prefix query to run against at all", "message": "Unexpected hierarchy expression in leaf operand" },
      { "action": "ts-window", "reason": "The runtime timestamp threshold carries sub-millisecond precision, which ordinary Elasticsearch date fields cannot preserve", "message": "Sub-millisecond timestamp literals require an explicit date_nanos mapping mode, which this adapter does not configure" },
      { "action": "ts-vf", "reason": "The value-first runtime timestamp threshold carries sub-millisecond precision, which ordinary Elasticsearch date fields cannot preserve", "message": "Sub-millisecond timestamp literals require an explicit date_nanos mapping mode, which this adapter does not configure" },
      { "action": "in-null-elem-mixed", "reason": "Elasticsearch does not index explicit null scalar values, so positive membership containing null cannot distinguish an explicit null attribute (true) from a missing attribute (CEL error)", "message": "Elasticsearch cannot distinguish an explicit null value from a missing field without an indexed null-value sentinel" },
      { "action": "in-null-elem-only", "reason": "The planner folds this to positive equality with null, which Elasticsearch cannot distinguish from a missing attribute without an indexed null-value sentinel", "message": "Elasticsearch cannot distinguish an explicit null value from a missing field without an indexed null-value sentinel" },
      { "action": "in-null-elem-rel", "reason": "Elasticsearch nested fields do not retain explicit null array elements, so null membership cannot be represented faithfully", "message": "null membership in a document array requires an explicit null-value mapping" },
      { "action": "in-null-elem-rel-neg", "reason": "Elasticsearch nested fields do not retain explicit null array elements, so negated null membership cannot be represented faithfully", "message": "Negated membership in a document collection cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "in-null-elem-hasint", "reason": "Elasticsearch nested fields do not retain explicit null array elements, so intersection with null cannot be represented faithfully", "message": "hasIntersection with null requires an explicit null-value mapping" },
      { "action": "in-var-var", "reason": "Elasticsearch Query DSL cannot test membership of one document field in another without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "in-var-var-neg", "reason": "Elasticsearch Query DSL cannot test negated membership of one document field in another without scripts", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "null-eq", "reason": "Elasticsearch does not index explicit null scalar values, so positive equality against null cannot distinguish an explicit null attribute (check() allows) from a missing field (CEL error, denies) without an indexed null_value sentinel. The negated forms (null-ne, vf-null-ne, null-not-eq) are expressible as exists and stay in the oracle set", "message": "Elasticsearch cannot distinguish an explicit null value from a missing field without an indexed null-value sentinel" },
      { "action": "cr-div-neg-zero", "reason": "Elasticsearch Query DSL cannot evaluate document-field division without scripts, whatever the denominator", "message": "Unexpected div expression in leaf operand" },
      { "action": "cr-div-other-column", "reason": "Elasticsearch Query DSL cannot evaluate division by a second document field without scripts", "message": "Unexpected div expression in leaf operand" },
      { "action": "cr-div-then-add", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic composed on a division without scripts", "message": "Unexpected add expression in leaf operand" },
      { "action": "cr-div-then-add-ne", "reason": "Same scriptless-arithmetic limitation as cr-div-then-add, reached through an inequality", "message": "Unexpected add expression in leaf operand" },
      { "action": "w1-all-chain", "reason": "Elasticsearch does not index empty nested arrays, so a positive all over a chained collection cannot distinguish an absent to-one parent (CEL error, denies) from an empty collection (vacuously true)", "message": "all cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "w1-not-exists-chain", "reason": "Elasticsearch does not index empty nested arrays, so a negated exists over a chained collection cannot distinguish an absent to-one parent from an empty collection", "message": "Negated exists cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "w1-size-zero-chain", "reason": "Same missing-versus-empty ambiguity as w1-not-exists-chain, reached through a zero-count emptiness check", "message": "size(request.resource.attr.mainCategory.subCategories) emptiness cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "w1-size-nonneg-chain", "reason": "Elasticsearch Query DSL expresses only emptiness checks over nested collections, not arbitrary count thresholds, so a >= 0 threshold — which is TRUE exactly when the to-one parent exists — has no equivalent", "message": "Unsupported size comparison: size(request.resource.attr.mainCategory.subCategories) ge 0.0. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "w1-not-in-chain", "reason": "Same missing-versus-empty ambiguity as w1-not-exists-chain, reached through negated membership: a bool must_not over a nested collection matches a document with no nested array at all, and Elasticsearch cannot tell that from a document whose array holds no match", "message": "Negated membership in a document collection cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "w1-not-hasint-chain", "reason": "Same missing-versus-empty ambiguity as w1-not-in-chain, reached through hasIntersection over the same operand", "message": "Negated hasIntersection cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "w1-not-size-chain", "reason": "Same missing-versus-empty ambiguity as w1-size-zero-chain, reached through the negated positive-count spelling rather than the direct zero-count one", "message": "size(request.resource.attr.mainCategory.subCategories) emptiness cannot distinguish a missing collection from an empty collection in Elasticsearch" },
      { "action": "w1-ternary-chain-cond", "reason": "Same missing conditional-value expression as ternary-bare: the Query DSL has no CEL-ternary equivalent without scripts, so the conditional is rejected at the top of the filter before its chained condition is examined", "message": "if requires exactly 2 operands, got 3" },
      { "action": "w1-size-frac-chain", "reason": "Same arbitrary-count-threshold limitation as w1-size-nonneg-chain, reached through a fractional threshold: >= 1.5 rounds up to >= 2, and the Query DSL expresses only emptiness over nested collections", "message": "Unsupported size comparison: size(request.resource.attr.mainCategory.subCategories) ge 1.5. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "w1-size-frac-le-chain", "reason": "Same arbitrary-count-threshold limitation as w1-size-frac-chain under the opposite ordering: <= 1.5 rounds down to <= 1, still not an emptiness check", "message": "Unsupported size comparison: size(request.resource.attr.mainCategory.subCategories) le 1.5. Only emptiness checks (size > 0, size == 0) are supported." },
      { "action": "null-value-ne-const", "reason": "Elasticsearch does not index a JSON null, so an explicitly-null value and a missing field are the same document to every query the DSL can express. Under the explicit convention CEL holds a null VALUE and allows the row, while every Elasticsearch spelling of `!= \"x\"` either requires the field to exist (dropping it) or matches every document missing the field. Neither is the decision, so the comparison is refused rather than answered narrowly", "message": "Elasticsearch cannot distinguish an explicit null value from a missing field without an indexed null-value sentinel" },
      { "action": "null-value-not-eq-const", "reason": "The negated-equality spelling reaches the same unrepresentable convention as null-value-ne-const", "message": "Elasticsearch cannot distinguish an explicit null value from a missing field without an indexed null-value sentinel" },
      { "action": "null-value-not-in-const", "reason": "The negated membership reaches the same unrepresentable convention as null-value-ne-const", "message": "Elasticsearch cannot distinguish an explicit null value from a missing field without an indexed null-value sentinel" },
      { "action": "null-value-pv-not-exists", "reason": "The value-list fold turns the macro into a chain of equalities against the explicit-null attribute, each of which reaches the same unrepresentable convention as null-value-ne-const", "message": "Elasticsearch cannot distinguish an explicit null value from a missing field without an indexed null-value sentinel" },
      { "action": "null-value-f2f", "reason": "Elasticsearch Query DSL compares a field against a literal only, so the field-to-field operand is rejected before either side's null convention is reached — the same limitation as field-to-field", "message": "Elasticsearch Query DSL cannot compare two document fields without scripts" }
    ]
  },
  "adapterSupportedExpected": {
    "mongoose": [
      { "action": "null-value-f2f-mixed", "reason": "Mongoose stores the value the caller sent, so a stored null compares as a null VALUE and a stored-null field is distinguishable from an absent one. The mixed conventions the SQL adapters cannot render in one predicate are both directly representable here: `nullable: true` on the omitted-side mapping makes its null behave as a missing attribute while the declared side keeps comparing as a value" },
      { "action": "p-matches", "reason": "MongoDB query filters natively support regular expressions" },
      { "action": "p-index", "reason": "MongoDB aggregation expressions support $arrayElemAt and $getField" },
      { "action": "p-timestamp", "reason": "MongoDB aggregation expressions can safely convert valid RFC 3339 strings to dates" }
    ],
    "elasticsearch-java": [
      { "action": "p-matches", "reason": "The ^h literal-prefix probe maps exactly to an Elasticsearch prefix query without crossing RE2/Lucene regex semantics" },
      { "action": "p-timestamp", "reason": "The whole-second timestamp probe is millisecond-exact and maps safely to an Elasticsearch date-field range query" }
    ],
    "convex": [
      { "action": "null-value-f2f-mixed", "reason": "Convex stores the value the caller sent, so a stored null compares as a null VALUE and a stored-null field is distinguishable from an absent one. The mixed conventions the SQL adapters cannot render in one predicate are both directly representable here" },
      { "action": "p-index", "reason": "Convex queries can apply the adapter's explicit post-filter for list indexing" },
      { "action": "p-matches", "reason": "Convex queries can apply the adapter's explicit post-filter for regular-expression matching" },
      { "action": "p-timestamp", "reason": "Convex queries can apply the adapter's explicit post-filter for timestamp comparisons" },
      { "action": "cast-int-string", "reason": "The adapter's explicit post-filter reimplements CEL int() over strings exactly (whole-string parse or error-deny), so the SQL leading-digits divergence does not apply" },
      { "action": "cast-double-string", "reason": "The adapter's explicit post-filter reimplements CEL double() over strings exactly (whole-string parse or error-deny), so the SQL leading-digits divergence does not apply" },
      { "action": "cast-int-double", "reason": "The adapter's explicit post-filter reimplements CEL int() truncation toward zero, so the SQL CAST round-to-nearest divergence does not apply" }
    ]
  },
  "expectedUnsupported": [
    {
      "action": "null-value-f2f-mixed",
      "shape": "field-to-field equality where one side declares the explicit-null convention and the other does not",
      "reason": "The declared side needs a definite answer for its NULL — CEL holds a null VALUE there. The undeclared side needs UNKNOWN for its NULL — that is a missing attribute, which CEL denies under BOTH polarities. A definite predicate returns rows the PDP refuses (e1, both columns NULL); a plain one drops rows the PDP allows (a2/a4/a8/c2). No single predicate is both, so every adapter refuses the shape rather than picking a direction. Declaring the convention on both attributes, or on neither, is expressible; mixing them across one comparison is not (cerbos/query-plan-adapters#308)",
      "messages": {
        "prisma": "between two columns under mixed null conventions",
        "drizzle": "between two columns under mixed null conventions",
        "sqlalchemy": "between two columns under mixed null conventions",
        "spring-data": "between two columns under mixed null conventions",
        "ent": "under mixed null conventions",
        "pgx": "under mixed null conventions",
        "langchain-chromadb": "Variable-to-variable comparisons are not supported by ChromaDB filters",
        "elasticsearch-java": "Elasticsearch Query DSL cannot compare two document fields without scripts",
        "activerecord": "between two columns under mixed null conventions"
      }
    },
    {
      "action": "p-timestamp",
      "shape": "timestamp() comparison over an ISO-date string column",
      "messages": {
        "prisma": "timestamp() field request.resource.attr.createdBy must be mapped with valueType: \"dateTime\"",
        "drizzle": "'timestamp' field 'request.resource.attr.createdBy' requires a mapping with valueType: \"timestamp\"",
        "langchain-chromadb": "Nested expressions are not supported by ChromaDB filters",
        "sqlalchemy": "timestamp() requires an RFC-3339 literal or a SQLAlchemy DateTime column",
        "ent": "timestamp() over a column requires that column to be mapped with a timestamp value type",
        "pgx": "timestamp() over a column requires that column to be mapped with a timestamp value type",
        "spring-data": "timestamp() comparison requires a column mapped to java.time.Instant or java.time.OffsetDateTime, but 'request.resource.attr.createdBy' maps to String. Other temporal representations (LocalDateTime, java.util.Date, String) are ambiguous about the absolute instant they store; remap the column or register an OperatorFunction override for 'lt'.",
        "activerecord": "timestamp() applied to a :string column: this adapter compares instants using the database's own temporal type"
      }
    },
    {
      "action": "p-matches",
      "shape": "matches() regex",
      "messages": {
        "prisma": "Unsupported operator: matches",
        "drizzle": "'matches' is not supported because SQL regex dialects do not guarantee CEL/RE2 semantics",
        "langchain-chromadb": "Unsupported operator matches",
        "sqlalchemy": "Unrecognised operator: matches",
        "ent": "unsupported operator: matches. CEL's regex dialect (RE2) is not the dialect any SQL engine implements, so a translated pattern would accept or reject different strings than the PDP",
        "pgx": "unsupported operator: matches. CEL's regex dialect (RE2) is not the dialect any SQL engine implements, so a translated pattern would accept or reject different strings than the PDP",
        "spring-data": "Unsupported operator: matches",
        "activerecord": "Unsupported operator: matches. Supply an operator override if the database can express it faithfully."
      }
    },
    {
      "action": "p-index",
      "shape": "list indexing (tags[0].name)",
      "messages": {
        "prisma": "Unsupported operator: get-field",
        "drizzle": "Unsupported value-expression operator: get-field",
        "langchain-chromadb": "Nested expressions are not supported by ChromaDB filters",
        "sqlalchemy": "Attribute 'request.resource.attr.tags' must be handled by an operator override or map to a SQLAlchemy column",
        "ent": "unsupported operator: get-field",
        "pgx": "unsupported operator: get-field",
        "elasticsearch-java": "Unexpected get-field expression in leaf operand",
        "spring-data": "Unexpected get-field() expression in leaf operand of eq",
        "activerecord": "Unsupported operator: index. Supply an operator override if the database can express it faithfully."
      }
    },
    {
      "action": "cast-int-string",
      "shape": "int() cast over a string column — CEL reads a whole string or raises, SQL reads the leading digits",
      "messages": {
        "prisma": "Unsupported operator: int",
        "mongoose": "'int()' cannot be translated: $convert parses a numeric prefix where CEL requires the whole string and raises otherwise, and rounds where CEL truncates toward zero",
        "drizzle": "Cannot translate int(): SQL CAST does not reproduce CEL conversion semantics — it reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero. The adapter rejects the shape instead of returning rows the PDP denies",
        "langchain-chromadb": "Nested expressions are not supported by ChromaDB filters",
        "sqlalchemy": "'int()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "ent": "'int()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "pgx": "'int()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "elasticsearch-java": "Unexpected int expression in leaf operand",
        "spring-data": "Unexpected int() expression in leaf operand of gt",
        "activerecord": "int() applied to a :string column: CEL reads the WHOLE string or makes an error, and Cerbos then denies the row, but SQL reads the digits at the front and gives a number"
      }
    },
    {
      "action": "cast-double-string",
      "shape": "double() cast over a string column — same whole-string-versus-leading-digits divergence as cast-int-string",
      "messages": {
        "prisma": "Unsupported operator: double",
        "mongoose": "'double()' cannot be translated: $convert parses a numeric prefix where CEL requires the whole string and raises otherwise, and rounds where CEL truncates toward zero",
        "drizzle": "Cannot translate double(): SQL CAST does not reproduce CEL conversion semantics — it reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero. The adapter rejects the shape instead of returning rows the PDP denies",
        "langchain-chromadb": "Nested expressions are not supported by ChromaDB filters",
        "sqlalchemy": "'double()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "ent": "'double()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "pgx": "'double()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "elasticsearch-java": "Unexpected double expression in leaf operand",
        "spring-data": "Unexpected double() expression in leaf operand of gt",
        "activerecord": "double() needs a numeric column. CEL makes an error for a string that is not a number, and Cerbos denies the row, but SQL gives 0.0 and the filter would keep it."
      }
    },
    {
      "action": "cast-int-double",
      "shape": "int() cast over a numeric column — CEL truncates toward zero, PostgreSQL and MySQL CAST round to nearest",
      "messages": {
        "prisma": "Unsupported operator: int",
        "mongoose": "'int()' cannot be translated: $convert parses a numeric prefix where CEL requires the whole string and raises otherwise, and rounds where CEL truncates toward zero",
        "drizzle": "Cannot translate int(): SQL CAST does not reproduce CEL conversion semantics — it reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero. The adapter rejects the shape instead of returning rows the PDP denies",
        "langchain-chromadb": "Nested expressions are not supported by ChromaDB filters",
        "sqlalchemy": "'int()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "ent": "'int()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "pgx": "'int()' cannot be lowered to SQL CAST: CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, and PostgreSQL and MySQL round where CEL truncates toward zero",
        "elasticsearch-java": "Unexpected int expression in leaf operand",
        "spring-data": "Unexpected int() expression in leaf operand of eq",
        "activerecord": "int() applied to a double column is not portable: CEL removes the fraction toward zero, and PostgreSQL and MySQL round a CAST to the nearest whole number instead"
      }
    },
    {
      "action": "filter-as-condition",
      "shape": "filter() used as the whole condition — it returns a list, not a boolean",
      "messages": {
        "prisma": "The filter() collection operator returns a list, not a boolean. It cannot be used as a standalone condition. Use exists() or combine filter() with size() instead.",
        "mongoose": "filter() returns a list, not a boolean, so it cannot be a condition on its own; only size(filter(...)) has a boolean meaning",
        "drizzle": "Cannot translate 'filter' as a condition: filter() returns a list, not a boolean. Only size(filter(...)) has a boolean meaning",
        "convex": "filter() returns a list, not a boolean, so it cannot be a condition on its own; only size() over its result has a boolean meaning",
        "langchain-chromadb": "Nested expressions are not supported by ChromaDB filters",
        "sqlalchemy": "the plan's condition translated to 'tuple', which is not a boolean SQL expression. filter() and map() return a list, so they cannot be a condition on their own (only size(filter(...)) has a boolean meaning), and an operator override returning an intermediate value must be consumed by an enclosing override before the root",
        "ent": "'filter' produces a collection rather than a boolean; it only translates inside size() or hasIntersection(), which give the collection a scalar meaning",
        "pgx": "'filter' produces a collection rather than a boolean; it only translates inside size() or hasIntersection(), which give the collection a scalar meaning",
        "elasticsearch-java": "Unexpected lambda expression in leaf operand",
        "spring-data": "filter() returns a list, not a boolean, so it cannot be a condition on its own; only size(filter(...)) has a boolean meaning",
        "activerecord": "a filtered relation cannot be used as a condition: CEL collection expressions such as filter() and map() evaluate to a list, not to a boolean"
      }
    },
    {
      "action": "map-as-condition",
      "shape": "map() used as the whole condition — it returns a list, not a boolean",
      "messages": {
        "prisma": "map() returns a list, not a boolean, so it cannot be a condition on its own; only comparisons or size() over its result have a boolean meaning",
        "mongoose": "map() returns a list, not a boolean, so it cannot be a condition on its own; only hasIntersection(map(...), [...]) gives the projection a boolean meaning",
        "drizzle": "Unsupported operator: map",
        "convex": "map() returns a list, not a boolean, so it cannot be a condition on its own; only size() over its result has a boolean meaning",
        "langchain-chromadb": "Nested expressions are not supported by ChromaDB filters",
        "sqlalchemy": "the plan's condition translated to 'tuple', which is not a boolean SQL expression. filter() and map() return a list, so they cannot be a condition on their own (only size(filter(...)) has a boolean meaning), and an operator override returning an intermediate value must be consumed by an enclosing override before the root",
        "ent": "'map' produces a collection rather than a boolean; it only translates inside size() or hasIntersection(), which give the collection a scalar meaning",
        "pgx": "'map' produces a collection rather than a boolean; it only translates inside size() or hasIntersection(), which give the collection a scalar meaning",
        "elasticsearch-java": "Unexpected lambda expression in leaf operand",
        "spring-data": "Unexpected lambda() expression in leaf operand of map",
        "activerecord": "a projected relation cannot be used as a condition: CEL collection expressions such as filter() and map() evaluate to a list, not to a boolean"
      }
    }
  ],
  "nullRepresentationOmitted": [
    {
      "action": "null-eq-missing",
      "reason": "`aOptionalString` follows the corpus default: a NULL column sends NO attribute, so `R.attr.aOptionalString == null` is a CEL missing-attribute error and check() denies all 21 seeds. The planner emits the same `eq(var, null)` node as `null-eq` (explicit null, 5 rows allowed), so the wire cannot tell the two conventions apart. Every adapter must be told which one the caller uses. Harnesses translate this action with the adapter's null representation set to `omitted` and assert a throw; under the default `explicit` the adapter would emit IS NULL and return the 5 rows the PDP denies (#302).",
      "messages": {
        "prisma": "missing-attribute error",
        "mongoose": "missing-attribute error",
        "drizzle": "missing-attribute error",
        "convex": "missing-attribute error",
        "langchain-chromadb": "finite number, string, or boolean literal",
        "sqlalchemy": "missing-attribute error",
        "ent": "missing-attribute error",
        "pgx": "missing-attribute error",
        "elasticsearch-java": "explicit null value from a missing field",
        "spring-data": "missing-attribute error",
        "activerecord": "Cannot translate a null constant with null_attribute_representation: :omitted"
      },
      "relatedIssue": "https://github.com/cerbos/query-plan-adapters/issues/302"
    }
  ],
  "knownDivergences": [
    {
      "action": "p-has",
      "adapters": ["spring-data", "prisma", "drizzle", "sqlalchemy", "mongoose", "langchain-chromadb", "elasticsearch-java", "convex", "ent", "pgx", "activerecord"],
      "reason": "The planner folds has(unknown attr) to KIND_ALWAYS_ALLOWED (a Cerbos planner bug, not an adapter bug), so the adapter returns all rows while check() denies rows where the attribute is missing. Excluded from the oracle run rather than asserted against.",
      "relatedIssue": null
    }
  ]
}
