{
  "description": "Every action defined in policies/adversarial.yaml, grouped against the Spring Data reference harness. 'conformance' actions must match the check() oracle. 'adapterUnsupported' lists conformance actions an adapter cannot express and must reject loudly. 'expectedUnsupported' lists shapes rejected by the reference; 'adapterSupportedExpected' records adapters that intentionally translate one of those shapes. 'nullRepresentationOmitted' lists actions that probe a `== null` comparison against an attribute the oracle OMITS for NULL columns; every adapter must translate these with its null representation set to 'omitted' and reject them. 'knownDivergences' records upstream planner behavior that prevents an oracle comparison.",
  "conformance": [
    "vf-le", "vf-ge", "vf-ne",
    "in-single", "in-empty",
    "like-percent", "like-underscore", "like-backslash",
    "unicode-eq", "empty-string-eq",
    "neg-number", "double-threshold",
    "all-on-empty", "exists-on-empty", "exists-one-multi", "not-exists",
    "outer-attr-depth2", "lambda-in-principal",
    "nary-and", "double-negation", "triple-negation", "not-empty",
    "optional-ne",
    "lambda-field-to-field", "field-to-field",
    "size-threshold", "size-filter-count", "string-size",
    "ternary-cmp", "ternary-expr-cond", "ternary-nested", "ternary-negated",
    "ternary-bare", "ternary-value-first", "ternary-null-cond",
    "f2f-contains", "f2f-startswith", "f2f-endswith",
    "arith-add", "arith-vf", "arith-sub", "arith-mult-neg",
    "arith-div", "arith-div-frac", "arith-both",
    "p-ternary-in-exists", "p-arith-in-lambda", "p-lambda-inner-f2f",
    "p-lambda-f2f-like", "p-size-nested",
    "p-ternary-of-ternaries", "p-ternary-vs-ternary", "p-ternary-under-all",
    "p-hasintersection-map", "p-deep-nest",
    "p-in-null-single", "p-in-null-multi", "p-startswith-concat",
    "p-struct", "p-not-exists-empty", "p-not-ternary-null", "p-double-frac",
    "n-not-exists-one-null", "n-all-mixed-null", "n-not-all-null", "n-not-all-absorb",
    "cr-contains", "cr-startswith", "cr-endswith", "cr-startswith-concat",
    "cr-div-zero", "cr-div-zero-ne", "cr-div-zero-eq-neg", "cr-size-frac-ge",
    "w1-exists-chain", "w1-size-chain", "w1-in-chain", "w2-outer-relation",
    "like-bracket", "cs-eq",
    "arith-add-eq-frac", "arith-add-ne-frac", "arith-add-eq-frac-exact",
    "size-huge-gt", "size-huge-lt",
    "nan-ord-ternary", "nan-ord-ternary-vf", "nan-ord-le", "nan-ord-inf",
    "macro-depth3-exists", "macro-depth3-not-exists", "macro-depth3-all",
    "pv-exists", "pv-all",
    "hier-ancestor-ff", "hier-ancestor-cf",
    "hier-descendent-ff", "hier-descendent-cf",
    "hier-overlaps-ff", "hier-overlaps-cf",
    "hier-meta-like", "hier-meta-in", "hier-overlaps-meta", "hier-bracket",
    "ts-window", "ts-vf", "ts-eq", "ts-eq-offset", "ts-ne",
    "in-null-elem-mixed", "in-null-elem-neg",
    "in-null-elem-only", "in-null-elem-only-neg",
    "in-null-elem-rel", "in-null-elem-rel-neg", "in-null-elem-hasint",
    "in-var-var", "in-var-var-neg",
    "null-eq", "null-ne", "vf-null-ne", "null-not-eq"
  ],
  "adapterUnsupported": {
    "prisma": [
      { "action": "like-percent", "reason": "Prisma emits LIKE without an ESCAPE clause and does not escape wildcards, so a needle containing % or _ cannot be matched literally; the adapter throws instead of filtering wrong" },
      { "action": "like-underscore", "reason": "Same LIKE-wildcard limitation as like-percent" },
      { "action": "p-startswith-concat", "reason": "The folded needle is '100%', hitting the same LIKE-wildcard limitation as like-percent" },
      { "action": "f2f-contains", "reason": "A column-valued needle would need per-row LIKE escaping, which Prisma filters cannot express; the adapter throws" },
      { "action": "f2f-startswith", "reason": "Same column-needle LIKE limitation as f2f-contains" },
      { "action": "f2f-endswith", "reason": "Same column-needle LIKE limitation as f2f-contains" },
      { "action": "lambda-field-to-field", "reason": "Prisma field references only work between fields of the same model; comparing an element column with an outer column is cross-model, so the adapter throws" },
      { "action": "p-lambda-f2f-like", "reason": "Cross-model comparison plus the column-needle LIKE limitation" },
      { "action": "p-deep-nest", "reason": "Contains a cross-model element-vs-outer column comparison (t.name == R.attr.aString)" },
      { "action": "arith-both", "reason": "Arithmetic on both sides of a comparison cannot be solved to a plain column filter; Prisma filters cannot express column arithmetic" },
      { "action": "cr-div-zero", "reason": "Division by a column cannot be solved to a plain column filter" },
      { "action": "cr-div-zero-ne", "reason": "Same column-division limitation as cr-div-zero, reached through an inequality" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same column-division limitation as cr-div-zero, reached through a negated equality" },
      { "action": "size-threshold", "reason": "Prisma where-filters cannot express relation-count thresholds (only empty/non-empty via none/some)" },
      { "action": "size-filter-count", "reason": "Same relation-count limitation as size-threshold" },
      { "action": "string-size", "reason": "Prisma has no string-length filter" },
      { "action": "p-size-nested", "reason": "Same relation-count limitation as size-threshold" },
      { "action": "cr-size-frac-ge", "reason": "Same relation-count limitation as size-threshold" },
      { "action": "w2-outer-relation", "reason": "Embeds a size(...) == 1 count threshold; same limitation as size-threshold" },
      { "action": "exists-one-multi", "reason": "exists_one requires counting matching elements, which Prisma where-filters cannot express; the adapter throws instead of silently degrading to exists" },
      { "action": "n-not-exists-one-null", "reason": "Same exists_one counting limitation" },
      { "action": "arith-add-eq-frac", "reason": "Prisma filters cannot express column arithmetic, and algebraically solving fractional IEEE-754 addition is not reversible for every row" },
      { "action": "arith-add-ne-frac", "reason": "Same fractional column-arithmetic limitation as arith-add-eq-frac" },
      { "action": "arith-add-eq-frac-exact", "reason": "An exact corpus witness does not make the general column-addition predicate expressible; Prisma has no column arithmetic" },
      { "action": "size-huge-gt", "reason": "Prisma has no string-length filter, so a giant size threshold remains inexpressible" },
      { "action": "size-huge-lt", "reason": "Same string-length limitation as size-huge-gt" },
      { "action": "hier-meta-like", "reason": "Hierarchy descendant prefixes require literal LIKE matching, but Prisma string filters cannot escape % or _; the adapter throws instead of treating them as wildcards" },
      { "action": "hier-overlaps-meta", "reason": "The inclusive overlap union ends in a descendant prefix LIKE on '50%:a_b:'; Prisma emits LIKE without an ESCAPE clause, so % and _ in that prefix stay wildcards and would wrongly admit 50x:a_b:y and 50%:aXb:y — the adapter throws instead" },
      { "action": "hier-bracket", "reason": "Hierarchy prefixes containing [ cannot be matched literally: Prisma emits LIKE without an ESCAPE clause, and [ opens a character class on SQL Server even when one is declared" },
      { "action": "in-var-var", "reason": "Comparing a related element column with an outer scalar column is cross-model, while Prisma field references are same-model only" },
      { "action": "in-var-var-neg", "reason": "Same cross-model correlated comparison limitation as in-var-var, under negation" },
      { "action": "ts-window", "reason": "The planner emits a sub-millisecond now() literal that Prisma DateTime cannot represent without changing the CEL instant" },
      { "action": "ts-vf", "reason": "Same sub-millisecond now() limitation in value-first form" }
    ],
    "drizzle": [
      { "action": "ts-window", "reason": "The planner emits a sub-millisecond now() literal that the documented Drizzle timestamp contract cannot represent without changing the CEL instant" },
      { "action": "ts-vf", "reason": "Same sub-millisecond now() limitation in value-first form" }
    ],
    "sqlalchemy": [
      { "action": "ts-window", "reason": "The planner emits a nanosecond-precision now() literal that Python/SQLAlchemy DateTime cannot represent without changing the CEL instant" },
      { "action": "ts-vf", "reason": "Same nanosecond-precision now() limitation in value-first form" }
    ],
    "mongoose": [
      { "action": "exists-one-multi", "reason": "exactly-one collection cardinality is not expressible by the current match-filter translator" },
      { "action": "n-not-exists-one-null", "reason": "same exact-cardinality limitation under negation" },
      { "action": "lambda-field-to-field", "reason": "MongoDB $expr cannot be safely scoped inside the adapter's $elemMatch predicates" },
      { "action": "outer-attr-depth2", "reason": "outer resource references inside nested element predicates require aggregation pipeline scoping" },
      { "action": "p-arith-in-lambda", "reason": "aggregation expressions inside $elemMatch predicates are unsupported" },
      { "action": "p-lambda-inner-f2f", "reason": "field-to-field aggregation expressions inside $elemMatch are unsupported" },
      { "action": "p-lambda-f2f-like", "reason": "dynamic string expressions inside $elemMatch are unsupported" },
      { "action": "p-size-nested", "reason": "nested collection size requires aggregation pipeline scoping" },
      { "action": "p-ternary-in-exists", "reason": "ternary aggregation expressions inside $elemMatch are unsupported" },
      { "action": "p-ternary-under-all", "reason": "ternary aggregation expressions inside $elemMatch are unsupported" },
      { "action": "p-ternary-vs-ternary", "reason": "Mongoose cannot cast a comparison between two conditional aggregation expressions" },
      { "action": "p-deep-nest", "reason": "contains an outer resource reference inside a collection predicate" },
      { "action": "size-filter-count", "reason": "size(filter(...)) requires an aggregation pipeline expression" },
      { "action": "w2-outer-relation", "reason": "combines nested collection size with an outer collection reference" },
      { "action": "in-var-var", "reason": "variable-in-variable membership requires correlated aggregation scoping" },
      { "action": "in-var-var-neg", "reason": "same variable-in-variable limitation under negation" },
      { "action": "cr-div-zero", "reason": "a row-dependent zero denominator aborts a Mongo query instead of denying that row" },
      { "action": "cr-div-zero-ne", "reason": "Same zero-denominator abort as cr-div-zero, reached through an inequality" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same zero-denominator abort as cr-div-zero, reached through a negated equality" },
      { "action": "nan-ord-ternary", "reason": "MongoDB division by zero aborts rather than producing CEL non-finite values" },
      { "action": "nan-ord-ternary-vf", "reason": "same non-finite arithmetic limitation in value-first form" },
      { "action": "nan-ord-le", "reason": "MongoDB cannot safely reproduce this CEL NaN ordering expression" },
      { "action": "nan-ord-inf", "reason": "MongoDB cannot safely reproduce this CEL infinity ordering expression" },
      { "action": "not-exists", "reason": "negating a nullable collection macro would turn CEL errors into matches" },
      { "action": "macro-depth3-not-exists", "reason": "same nullable macro-negation limitation across three collection levels" },
      { "action": "n-not-all-null", "reason": "negated all over nullable element members needs explicit three-valued logic" },
      { "action": "n-not-all-absorb", "reason": "same nullable all-negation limitation with an absorbing false witness" },
      { "action": "p-not-exists-empty", "reason": "negated exists over nullable element members needs explicit three-valued logic" },
      { "action": "p-not-ternary-null", "reason": "negating a ternary over a nullable field cannot preserve CEL error semantics" },
      { "action": "ts-window", "reason": "the planner emits a nanosecond-precision now() literal that BSON Date cannot represent without changing the CEL instant" },
      { "action": "ts-vf", "reason": "same nanosecond-precision now() limitation in value-first form" }
    ],
    "langchain-chromadb": [
      { "action": "like-percent", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it" },
      { "action": "like-underscore", "reason": "Chroma metadata filters have no substring or pattern operator, so contains has no faithful Where equivalent and the adapter rejects it" },
      { "action": "like-backslash", "reason": "Chroma metadata filters have no suffix or pattern operator, so endsWith has no faithful Where equivalent and the adapter rejects it" },
      { "action": "double-threshold", "reason": "Chroma distinguishes integer and floating-point metadata; a fractional threshold against this integer field is not faithful" },
      { "action": "all-on-empty", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "exists-on-empty", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "exists-one-multi", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "not-exists", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one" },
      { "action": "outer-attr-depth2", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "lambda-in-principal", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "not-empty", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated" },
      { "action": "optional-ne", "reason": "Chroma $ne includes records with missing metadata, unlike Cerbos missing-attribute error semantics" },
      { "action": "lambda-field-to-field", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "field-to-field", "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated" },
      { "action": "size-threshold", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated" },
      { "action": "size-filter-count", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated" },
      { "action": "string-size", "reason": "Chroma metadata filters have no string-length function, and every comparison operand must be a bare field or literal, so size(field) cannot be an operand" },
      { "action": "ternary-cmp", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "ternary-expr-cond", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "ternary-nested", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "ternary-negated", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "ternary-bare", "reason": "The bare ternary plans as a three-operand conditional at the top of the filter; Chroma's Where model only takes two-operand field-vs-literal comparisons" },
      { "action": "ternary-value-first", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "ternary-null-cond", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "f2f-contains", "reason": "Chroma has no field-to-field operand and no substring or pattern operator, so a column-valued needle is doubly inexpressible" },
      { "action": "f2f-startswith", "reason": "Chroma has no field-to-field operand and no substring or pattern operator, so a column-valued needle is doubly inexpressible" },
      { "action": "f2f-endswith", "reason": "Chroma has no field-to-field operand and no substring or pattern operator, so a column-valued needle is doubly inexpressible" },
      { "action": "arith-add", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-vf", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-sub", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-mult-neg", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-div", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-div-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-both", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "p-ternary-in-exists", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "p-arith-in-lambda", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "p-lambda-inner-f2f", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "p-lambda-f2f-like", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "p-size-nested", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated" },
      { "action": "p-ternary-of-ternaries", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "p-ternary-vs-ternary", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "p-ternary-under-all", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "p-hasintersection-map", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "p-deep-nest", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "p-startswith-concat", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it" },
      { "action": "p-not-exists-empty", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one" },
      { "action": "p-not-ternary-null", "reason": "Chroma metadata filters have no conditional operand: the plan's ternary must be evaluated per record before comparison, which a static Where document cannot express" },
      { "action": "p-double-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "n-not-exists-one-null", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one" },
      { "action": "n-all-mixed-null", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "n-not-all-null", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one" },
      { "action": "n-not-all-absorb", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one" },
      { "action": "cr-contains", "reason": "Chroma metadata filters have no substring or pattern operator, so contains has no faithful Where equivalent and the adapter rejects it" },
      { "action": "cr-startswith", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it" },
      { "action": "cr-endswith", "reason": "Chroma metadata filters have no suffix or pattern operator, so endsWith has no faithful Where equivalent and the adapter rejects it" },
      { "action": "cr-startswith-concat", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it" },
      { "action": "cr-div-zero", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "cr-div-zero-ne", "reason": "Same flat-scalar-metadata limitation as cr-div-zero: the arithmetic operand cannot be translated at all, whatever the enclosing comparison" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same flat-scalar-metadata limitation as cr-div-zero under a negated equality" },
      { "action": "cr-size-frac-ge", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated" },
      { "action": "w1-exists-chain", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "w1-size-chain", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated" },
      { "action": "w1-in-chain", "reason": "Chroma's $in tests a metadata field against a literal list; it cannot test whether a literal is a member of a list-valued metadata field, and Chroma metadata values are scalars anyway" },
      { "action": "w2-outer-relation", "reason": "Chroma metadata is a flat scalar map with no relation model and no count function, so a size(...) collection-count operand cannot be translated" },
      { "action": "like-bracket", "reason": "Chroma metadata filters expose only scalar comparison and membership operators ($eq/$ne/$lt/$lte/$gt/$gte/$in/$nin) \u2014 there is no prefix, substring, or pattern operator \u2014 so startsWith has no faithful Where equivalent and the adapter rejects it" },
      { "action": "arith-add-eq-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-add-ne-frac", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "arith-add-eq-frac-exact", "reason": "Chroma metadata filters compare a stored field against a literal only \u2014 there is no computed-value or column-arithmetic operand \u2014 so the plan's arithmetic expression cannot be translated" },
      { "action": "size-huge-gt", "reason": "Chroma metadata filters have no string-length function, and every comparison operand must be a bare field or literal, so size(field) cannot be an operand" },
      { "action": "size-huge-lt", "reason": "Chroma metadata filters have no string-length function, and every comparison operand must be a bare field or literal, so size(field) cannot be an operand" },
      { "action": "nan-ord-ternary", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated" },
      { "action": "nan-ord-ternary-vf", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated" },
      { "action": "nan-ord-le", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated" },
      { "action": "nan-ord-inf", "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated" },
      { "action": "macro-depth3-exists", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "macro-depth3-not-exists", "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one" },
      { "action": "macro-depth3-all", "reason": "Chroma metadata is a flat scalar map with no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated" },
      { "action": "pv-exists", "reason": "Chroma has no collection-macro operator to fold the lambda into: above the planner's 10-element unroll cap the collection arrives as a literal value list with a lambda body, and $in matches a field against a list rather than evaluating a per-element predicate. Write membership as `R.attr.x in P.attr.xs`, which the planner emits as `in` + value list at every collection size and maps to a single $in" },
      { "action": "pv-all", "reason": "Same missing collection-macro operator as pv-exists; additionally the unrolled form is a chain of $ne, which Chroma can only express safely when the mapper declares the metadata key required, since a document missing the key matches $ne" },
      { "action": "hier-ancestor-ff", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-ancestor-cf", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-descendent-ff", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-descendent-cf", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-overlaps-ff", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-overlaps-cf", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-meta-like", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-meta-in", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-overlaps-meta", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "hier-bracket", "reason": "Chroma metadata filters have no path or prefix operator, and every comparison operand must be a bare field or literal, so the plan's hierarchy(...) operands cannot be translated" },
      { "action": "ts-window", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated" },
      { "action": "ts-vf", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated" },
      { "action": "ts-eq", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated" },
      { "action": "ts-eq-offset", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated" },
      { "action": "ts-ne", "reason": "Chroma metadata values are strings, numbers, and booleans only \u2014 there is no temporal type and no timestamp(...) operand \u2014 so the plan's instant comparison cannot be translated" },
      { "action": "in-null-elem-mixed", "reason": "Chroma metadata values are non-null scalars, so a membership list containing null cannot be represented and the adapter refuses to silently drop the null element" },
      { "action": "in-null-elem-neg", "reason": "Chroma $nin matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required" },
      { "action": "in-null-elem-only", "reason": "The planner folds membership in [null] to an equality against null; Chroma metadata has no null value, so the comparison cannot be represented" },
      { "action": "in-null-elem-only-neg", "reason": "Chroma $ne matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required" },
      { "action": "in-null-elem-rel", "reason": "Chroma's $in tests a metadata field against a literal list; it cannot test whether a literal is a member of a list-valued metadata field, and Chroma metadata values are scalars anyway" },
      { "action": "in-null-elem-rel-neg", "reason": "Chroma's $in tests a metadata field against a literal list; it cannot test whether a literal is a member of a list-valued metadata field, and Chroma metadata values are scalars anyway" },
      { "action": "in-null-elem-hasint", "reason": "Chroma metadata values are scalars with no list-intersection operator, so hasIntersection has no Where equivalent" },
      { "action": "in-var-var", "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated" },
      { "action": "in-var-var-neg", "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated" },
      { "action": "null-eq", "reason": "Chroma $eq accepts only a finite number, string or boolean literal, and Chroma metadata has no null value, so an equality against null cannot be represented" },
      { "action": "null-ne", "reason": "Chroma $ne matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required" },
      { "action": "vf-null-ne", "reason": "Value-first form of null-ne; the same $ne absent-key mismatch applies after the planner mirrors the operands" },
      { "action": "null-not-eq", "reason": "Negating the null equality routes through the same $ne absent-key mismatch as null-ne" }
    ],
    "elasticsearch-java": [
      { "action": "all-on-empty", "reason": "Elasticsearch does not index empty nested arrays, so a positive all predicate cannot distinguish an empty collection (true) from a missing collection (CEL error)" },
      { "action": "exists-one-multi", "reason": "Elasticsearch Query DSL cannot count nested matches to express exists_one without scripts" },
      { "action": "not-exists", "reason": "Elasticsearch does not index empty nested arrays, so negated exists cannot distinguish an empty collection (true) from a missing collection (CEL error)" },
      { "action": "outer-attr-depth2", "reason": "Elasticsearch nested queries cannot preserve a resource-field correlation across two nested scopes without scripts" },
      { "action": "lambda-field-to-field", "reason": "Elasticsearch Query DSL cannot compare a nested field with an outer resource field without scripts" },
      { "action": "field-to-field", "reason": "Elasticsearch Query DSL cannot compare two document fields without scripts" },
      { "action": "size-threshold", "reason": "Elasticsearch Query DSL cannot express arbitrary collection-count thresholds without scripts" },
      { "action": "size-filter-count", "reason": "Elasticsearch Query DSL cannot count only nested elements matching a predicate without scripts" },
      { "action": "string-size", "reason": "Elasticsearch Query DSL cannot compare a string's length without scripts" },
      { "action": "ternary-cmp", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts" },
      { "action": "ternary-expr-cond", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts" },
      { "action": "ternary-nested", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts" },
      { "action": "ternary-negated", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts" },
      { "action": "ternary-bare", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts" },
      { "action": "ternary-value-first", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts" },
      { "action": "ternary-null-cond", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to CEL ternary without scripts" },
      { "action": "f2f-contains", "reason": "Elasticsearch Query DSL cannot use one document field as another field's string-search needle without scripts" },
      { "action": "f2f-startswith", "reason": "Elasticsearch Query DSL cannot use one document field as another field's string-search needle without scripts" },
      { "action": "f2f-endswith", "reason": "Elasticsearch Query DSL cannot use one document field as another field's string-search needle without scripts" },
      { "action": "arith-add", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts" },
      { "action": "arith-vf", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts" },
      { "action": "arith-sub", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts" },
      { "action": "arith-mult-neg", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts" },
      { "action": "arith-div", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts" },
      { "action": "arith-div-frac", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts" },
      { "action": "arith-both", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic without scripts" },
      { "action": "p-ternary-in-exists", "reason": "Elasticsearch Query DSL cannot evaluate a conditional value inside a nested predicate without scripts" },
      { "action": "p-arith-in-lambda", "reason": "Elasticsearch Query DSL cannot evaluate arithmetic inside a nested predicate without scripts" },
      { "action": "p-lambda-inner-f2f", "reason": "Elasticsearch Query DSL cannot compare fields within a nested element without scripts" },
      { "action": "p-lambda-f2f-like", "reason": "Elasticsearch Query DSL cannot use an outer document field as a nested string-search needle without scripts" },
      { "action": "p-size-nested", "reason": "Elasticsearch Query DSL cannot express arbitrary nested collection-count thresholds without scripts" },
      { "action": "p-ternary-of-ternaries", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to nested CEL ternaries without scripts" },
      { "action": "p-ternary-vs-ternary", "reason": "Elasticsearch Query DSL has no conditional-value expression equivalent to nested CEL ternaries without scripts" },
      { "action": "p-ternary-under-all", "reason": "Elasticsearch Query DSL cannot evaluate a conditional value inside a nested all predicate without scripts" },
      { "action": "p-deep-nest", "reason": "This predicate combines field arithmetic and nested-to-outer field comparison, neither expressible by Elasticsearch Query DSL without scripts" },
      { "action": "p-not-exists-empty", "reason": "Elasticsearch does not index empty nested arrays, so negated exists cannot distinguish an empty collection (true) from a missing collection (CEL error)" },
      { "action": "p-not-ternary-null", "reason": "Elasticsearch Query DSL cannot preserve CEL null/error semantics through a negated conditional value without scripts" },
      { "action": "p-double-frac", "reason": "Elasticsearch Query DSL cannot evaluate document-field arithmetic with fractional values without scripts" },
      { "action": "n-all-mixed-null", "reason": "Positive all is unsafe because Elasticsearch cannot distinguish an empty nested collection from a missing collection, in addition to the nullable-element CEL error semantics this action exercises" },
      { "action": "n-not-exists-one-null", "reason": "Elasticsearch Query DSL cannot count nested matches or preserve exists_one null/error semantics without scripts" },
      { "action": "cr-contains", "reason": "This value-first string predicate uses a document field as the search needle against a constant haystack, which Elasticsearch Query DSL cannot express without scripts" },
      { "action": "cr-startswith", "reason": "This value-first string predicate uses a document field as the search needle against a constant haystack, which Elasticsearch Query DSL cannot express without scripts" },
      { "action": "cr-endswith", "reason": "This value-first string predicate uses a document field as the search needle against a constant haystack, which Elasticsearch Query DSL cannot express without scripts" },
      { "action": "cr-startswith-concat", "reason": "After constant folding, this value-first predicate still uses a document field as the search needle against a constant haystack, which requires scripts" },
      { "action": "cr-div-zero", "reason": "Elasticsearch Query DSL cannot evaluate division by a document field without scripts" },
      { "action": "cr-div-zero-ne", "reason": "Elasticsearch Query DSL cannot evaluate division by a document field without scripts, whatever the enclosing comparison" },
      { "action": "cr-div-zero-eq-neg", "reason": "Same scriptless-division limitation as cr-div-zero under a negated equality" },
      { "action": "cr-size-frac-ge", "reason": "Elasticsearch Query DSL cannot compare collection counts against arbitrary thresholds without scripts" },
      { "action": "w2-outer-relation", "reason": "Elasticsearch Query DSL cannot correlate nested traversal with an outer relation-count predicate without scripts" },
      { "action": "arith-add-eq-frac", "reason": "Elasticsearch Query DSL cannot evaluate IEEE-754 document-field addition without scripts" },
      { "action": "arith-add-ne-frac", "reason": "Elasticsearch Query DSL cannot evaluate IEEE-754 document-field addition without scripts" },
      { "action": "arith-add-eq-frac-exact", "reason": "Elasticsearch Query DSL cannot evaluate IEEE-754 document-field addition without scripts" },
      { "action": "size-huge-gt", "reason": "Elasticsearch Query DSL cannot compare a string's length without scripts" },
      { "action": "size-huge-lt", "reason": "Elasticsearch Query DSL cannot compare a string's length without scripts" },
      { "action": "nan-ord-ternary", "reason": "Elasticsearch Query DSL cannot reproduce CEL NaN ordering through conditional values without scripts" },
      { "action": "nan-ord-ternary-vf", "reason": "Elasticsearch Query DSL cannot reproduce CEL NaN ordering through conditional values without scripts" },
      { "action": "nan-ord-le", "reason": "Elasticsearch Query DSL cannot evaluate the non-finite arithmetic operand required by this CEL predicate without scripts" },
      { "action": "nan-ord-inf", "reason": "Elasticsearch Query DSL cannot evaluate the non-finite arithmetic operand required by this CEL predicate without scripts" },
      { "action": "macro-depth3-exists", "reason": "Elasticsearch Query DSL cannot preserve three levels of nested collection scope with the configured mappings" },
      { "action": "macro-depth3-not-exists", "reason": "Elasticsearch Query DSL cannot preserve three levels of nested collection scope and CEL error semantics under negation" },
      { "action": "macro-depth3-all", "reason": "Elasticsearch Query DSL cannot preserve three levels of nested collection scope and CEL all semantics" },
      { "action": "hier-ancestor-ff", "reason": "The adapter does not translate CEL hierarchy relationships into Elasticsearch Query DSL" },
      { "action": "hier-ancestor-cf", "reason": "The adapter does not translate CEL hierarchy relationships into Elasticsearch Query DSL" },
      { "action": "hier-descendent-ff", "reason": "The adapter does not translate CEL hierarchy relationships into Elasticsearch Query DSL" },
      { "action": "hier-descendent-cf", "reason": "The adapter does not translate CEL hierarchy relationships into Elasticsearch Query DSL" },
      { "action": "hier-overlaps-ff", "reason": "The adapter does not translate CEL hierarchy relationships into Elasticsearch Query DSL" },
      { "action": "hier-overlaps-cf", "reason": "The adapter does not translate CEL hierarchy relationships into Elasticsearch Query DSL" },
      { "action": "hier-meta-like", "reason": "The adapter does not translate CEL hierarchy metadata operations into Elasticsearch Query DSL" },
      { "action": "hier-meta-in", "reason": "The adapter does not translate CEL hierarchy metadata operations into Elasticsearch Query DSL" },
      { "action": "hier-overlaps-meta", "reason": "The adapter has no hierarchy(...) operand handling at all, so the metacharacter overlap union never reaches a prefix query; it is rejected for the same reason as the other hierarchy shapes" },
      { "action": "hier-bracket", "reason": "The adapter does not translate CEL hierarchy indexing into Elasticsearch Query DSL" },
      { "action": "ts-window", "reason": "The runtime timestamp threshold carries sub-millisecond precision, which ordinary Elasticsearch date fields cannot preserve" },
      { "action": "ts-vf", "reason": "The value-first runtime timestamp threshold carries sub-millisecond precision, which ordinary Elasticsearch date fields cannot preserve" },
      { "action": "in-null-elem-mixed", "reason": "Elasticsearch does not index explicit null scalar values, so positive membership containing null cannot distinguish an explicit null attribute (true) from a missing attribute (CEL error)" },
      { "action": "in-null-elem-only", "reason": "The planner folds this to positive equality with null, which Elasticsearch cannot distinguish from a missing attribute without an indexed null-value sentinel" },
      { "action": "in-null-elem-rel", "reason": "Elasticsearch nested fields do not retain explicit null array elements, so null membership cannot be represented faithfully" },
      { "action": "in-null-elem-rel-neg", "reason": "Elasticsearch nested fields do not retain explicit null array elements, so negated null membership cannot be represented faithfully" },
      { "action": "in-null-elem-hasint", "reason": "Elasticsearch nested fields do not retain explicit null array elements, so intersection with null cannot be represented faithfully" },
      { "action": "in-var-var", "reason": "Elasticsearch Query DSL cannot test membership of one document field in another without scripts" },
      { "action": "in-var-var-neg", "reason": "Elasticsearch Query DSL cannot test negated membership of one document field in another without scripts" },
      { "action": "null-eq", "reason": "Elasticsearch does not index explicit null scalar values, so positive equality against null cannot distinguish an explicit null attribute (check() allows) from a missing field (CEL error, denies) without an indexed null_value sentinel. The negated forms (null-ne, vf-null-ne, null-not-eq) are expressible as exists and stay in the oracle set" }
    ]
  },
  "adapterSupportedExpected": {
    "mongoose": [
      { "action": "p-matches", "reason": "MongoDB query filters natively support regular expressions" },
      { "action": "p-index", "reason": "MongoDB aggregation expressions support $arrayElemAt and $getField" },
      { "action": "p-timestamp", "reason": "MongoDB aggregation expressions can safely convert valid RFC 3339 strings to dates" }
    ],
    "elasticsearch-java": [
      { "action": "p-matches", "reason": "The ^h literal-prefix probe maps exactly to an Elasticsearch prefix query without crossing RE2/Lucene regex semantics" },
      { "action": "p-timestamp", "reason": "The whole-second timestamp probe is millisecond-exact and maps safely to an Elasticsearch date-field range query" }
    ],
    "convex": [
      { "action": "p-index", "reason": "Convex queries can apply the adapter's explicit post-filter for list indexing" },
      { "action": "p-matches", "reason": "Convex queries can apply the adapter's explicit post-filter for regular-expression matching" },
      { "action": "p-timestamp", "reason": "Convex queries can apply the adapter's explicit post-filter for timestamp comparisons" }
    ]
  },
  "expectedUnsupported": [
    { "action": "p-timestamp", "shape": "timestamp() comparison over an ISO-date string column", "springDataMessage": "Unexpected timestamp() expression in leaf operand of lt" },
    { "action": "p-matches",   "shape": "matches() regex", "springDataMessage": "Unsupported operator: matches" },
    { "action": "p-index",     "shape": "list indexing (tags[0].name)", "springDataMessage": "Unexpected get-field() expression in leaf operand of eq" }
  ],
  "nullRepresentationOmitted": [
    {
      "action": "null-eq-missing",
      "reason": "`aOptionalString` follows the corpus default: a NULL column sends NO attribute, so `R.attr.aOptionalString == null` is a CEL missing-attribute error and check() denies all 20 seeds. The planner emits the same `eq(var, null)` node as `null-eq` (explicit null, 5 rows allowed), so the wire cannot tell the two conventions apart. Every adapter must be told which one the caller uses. Harnesses translate this action with the adapter's null representation set to `omitted` and assert a throw; under the default `explicit` the adapter would emit IS NULL and return the 5 rows the PDP denies (#302).",
      "relatedIssue": "https://github.com/cerbos/query-plan-adapters/issues/302"
    }
  ],
  "knownDivergences": [
    {
      "action": "p-has",
      "adapters": ["spring-data", "prisma", "drizzle", "sqlalchemy", "mongoose", "langchain-chromadb", "elasticsearch-java", "convex", "ent", "pgx"],
      "reason": "The planner folds has(unknown attr) to KIND_ALWAYS_ALLOWED (a Cerbos planner bug, not an adapter bug), so the adapter returns all rows while check() denies rows where the attribute is missing. Excluded from the oracle run rather than asserted against.",
      "relatedIssue": null
    }
  ]
}
