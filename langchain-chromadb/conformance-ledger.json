{
  "adapter": "langchain-chromadb",
  "cases": {
    "arithmetic/add/field-plus-constant": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `add` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/add/on-both-sides": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `add` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/add/self-division-plus-constant-greater-than": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `arithmetic/divide/self-division-greater-than`, with addition composed on the division. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/add/self-division-plus-constant-not-equals": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `arithmetic/add/self-division-plus-constant-greater-than`, reached through an inequality. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/add/value-first": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `add` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/divide/field-by-field": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `arithmetic/divide/self-division-greater-than`, with a second metadata key as the denominator — Chroma has neither arithmetic nor a field-to-field operand. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/divide/fractional-quotient": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `div` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/divide/fractional-threshold": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `div` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/divide/negated-self-division-equals": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `arithmetic/divide/self-division-greater-than` under a negated equality. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/divide/negative-zero-divisor": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `arithmetic/divide/self-division-greater-than`: Chroma compares a stored field against a literal only, so a division operand cannot be translated whatever its denominator. `allowPostFilter` does not lift it: `div` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/divide/nested-division-divisor": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/divide/self-division-greater-than": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/divide/self-division-not-equals": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `arithmetic/divide/self-division-greater-than`: the arithmetic operand cannot be translated at all, whatever the enclosing comparison. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/multiply/inexact-fraction-equals": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `mult` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/multiply/nan-times-zero-plus-column": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: the divisor reads metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost and IEEE division by it would give the wrong infinity."
    },
    "arithmetic/multiply/negative-multiplier": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `mult` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "arithmetic/subtract/field-minus-constant": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a stored field against a literal only — there is no computed-value or column-arithmetic operand — so the plan's arithmetic expression cannot be translated. `allowPostFilter` does not lift it: `sub` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "cast/string/from-double": {
      "status": "unsupported",
      "reason": "Every Chroma comparison operand must be a bare metadata field or a literal. string() wraps the field in an expression, and Chroma's Where model has no computed operand at all — there is no cast, and no place to put one. `allowPostFilter` does not lift it: `string()` reads a number from metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost where `string()` renders -0.0 as \"-0\"."
    },
    "cast/string/from-double-spellings": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `cast/string/from-double`: Chroma has no cast, so the double-to-string spellings this action probes are never reached. `allowPostFilter` does not lift it: `string()` reads a number from metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost where `string()` renders -0.0 as \"-0\"."
    },
    "cast/string/from-negative-zero-double": {
      "status": "unsupported",
      "reason": "Same missing computed operand as `cast/string/from-double`, reached through the negative-zero spelling. `allowPostFilter` does not lift it: `string()` reads a number from metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost where `string()` renders -0.0 as \"-0\"."
    },
    "cast/string/negated-explicit-null": {
      "status": "unsupported",
      "reason": "Chroma metadata filters expose neither computed scalar expressions nor regex matching. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "cast/string/negated-missing-attribute": {
      "status": "unsupported",
      "reason": "Chroma metadata filters expose neither computed scalar expressions nor regex matching. `allowPostFilter` does not lift it: `string()` reads a number from metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost where `string()` renders -0.0 as \"-0\"."
    },
    "collection/all/empty-collection": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/all/nested-exists-over-same-collection": {
      "status": "unsupported",
      "reason": "Same as `collection/exists/nested-over-same-collection`, reached through all(). `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/all/nested-three-levels-middle": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.categories` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/except/as-whole-condition": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no except operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the post-filter has no evaluation for `except`."
    },
    "collection/except/equals-list-literal": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `except`."
    },
    "collection/except/size-of-difference": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `except`."
    },
    "collection/exists-one/several-matches": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/arithmetic-on-outer-attribute": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/element-contains-outer-attribute": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/element-field-to-element-field": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/element-field-to-outer-field": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/element-in-literal-list": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/element-in-principal-list": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/empty-collection": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/negated": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/negated-element-in-literal-list": {
      "status": "unsupported",
      "reason": "Chroma metadata filters cannot negate the retained exists expression; there is no `$not`, and the macro has no Where form to invert. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/negated-empty-collection": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/negated-nested-over-same-collection": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/negated-nested-three-levels": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one. `allowPostFilter` does not lift it: `R.attr.categories` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/nested-collection-size": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.categories` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/nested-over-same-collection": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, so a collection macro — let alone one nested inside another — has no Where form. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/nested-three-levels": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.categories` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/nested-with-outer-attribute": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.categories` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/outer-collection-inside-lambda": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.categories` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/scalar-list-equals": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements, so the collection lambda is a nested expression with no Where representation. `$contains` tests list membership, but the adapter does not translate it (#475). `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/exists/scalar-list-negated-body": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements, so the collection lambda is a nested expression with no Where representation. `$contains` tests list membership, but the adapter does not translate it (#475). `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/filter/as-conjunct": {
      "status": "unsupported",
      "reason": "An operand is a computed expression, and a Chroma `Where` clause can only compare a metadata key with a literal. `allowPostFilter` does not lift it: `filter()` returns a list, not a boolean: CEL rejects it as a condition, and evaluating it would give the shape a meaning the policy never stated."
    },
    "collection/filter/as-whole-condition": {
      "status": "unsupported",
      "reason": "An operand is a computed expression, and a Chroma `Where` clause can only compare a metadata key with a literal. `allowPostFilter` does not lift it: `filter()` returns a list, not a boolean: CEL rejects it as a condition, and evaluating it would give the shape a meaning the policy never stated."
    },
    "collection/has-intersection/mapped-names-value-first": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/has-intersection/mapped-names-with-metacharacters": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/has-intersection/value-first": {
      "status": "unsupported",
      "reason": "hasIntersection is not in the adapter's operator set: Chroma has no list-intersection operator, only per-element `$contains`, which the adapter does not translate (#475) because Chroma stores an empty list as an absent key and rejects null elements and mixed types. Mirroring the operands does not change that. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/first-element-of-boolean-list": {
      "status": "unsupported",
      "reason": "Same missing positional access as `collection/index/first-element-of-number-list`, over a boolean list. `allowPostFilter` does not lift it: `R.attr.aBoolList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/first-element-of-number-list": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no positional element access. The pinned server stores a homogeneous list as metadata but filters it only by position-blind membership ($contains matches [3, 2] as readily as [2]), stores an empty list as an absent key, and rejects the null elements these seeds carry. The index() node is a computed operand where the comparison walk expects a bare metadata key, so it is refused as a nested expression before the element's JSON type could matter. `allowPostFilter` does not lift it: `R.attr.aNumberList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/first-element-of-object-list": {
      "status": "unsupported",
      "reason": "An operand is a computed expression, and a Chroma `Where` clause can only compare a metadata key with a literal. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/first-element-of-string-list": {
      "status": "unsupported",
      "reason": "Same missing positional access as `collection/index/first-element-of-number-list`, over a string list: `$eq` on a list key and a dotted `key.N` both match nothing, and the index() node is refused as a nested expression before its comparison is examined. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/first-element-of-string-list-equals-null": {
      "status": "unsupported",
      "reason": "Same missing positional access as `collection/index/first-element-of-number-list`, over a string list: `$eq` on a list key and a dotted `key.N` both match nothing, and the index() node is refused as a nested expression before its comparison is examined. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/fractional-position": {
      "status": "unsupported",
      "reason": "Chroma metadata filters expose neither computed scalar expressions nor regex matching. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/negated-first-element-of-boolean-list": {
      "status": "unsupported",
      "reason": "Same missing positional access as `collection/index/first-element-of-boolean-list`: the negation is pushed down to the comparison, which refuses the index() operand before choosing an inverted operator. `allowPostFilter` does not lift it: `R.attr.aBoolList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/negated-first-element-of-number-list": {
      "status": "unsupported",
      "reason": "Same missing positional access as `collection/index/first-element-of-number-list`: the negation is pushed down to the comparison, which refuses the index() operand before choosing an inverted operator. `allowPostFilter` does not lift it: `R.attr.aNumberList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/negated-first-element-of-string-list": {
      "status": "unsupported",
      "reason": "Same missing positional access as `collection/index/first-element-of-number-list`, over a string list: `$eq` on a list key and a dotted `key.N` both match nothing, and the index() node is refused as a nested expression before its comparison is examined. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/negated-out-of-bounds": {
      "status": "unsupported",
      "reason": "Chroma metadata filters expose neither computed scalar expressions nor regex matching. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/index/negative-position": {
      "status": "unsupported",
      "reason": "Chroma metadata filters expose neither computed scalar expressions nor regex matching. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "collection/map/as-whole-condition": {
      "status": "unsupported",
      "reason": "An operand is a computed expression, and a Chroma `Where` clause can only compare a metadata key with a literal. `allowPostFilter` does not lift it: `map()` returns a list, not a boolean: CEL rejects it as a condition, and evaluating it would give the shape a meaning the policy never stated."
    },
    "collection/map/equals-list-literal": {
      "status": "unsupported",
      "reason": "A map() projection is a nested expression in an operand position, and Chroma has no relation to project from. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "comparison/equals/whole-list-literal": {
      "status": "unsupported",
      "reason": "eq requires a finite number, string, or boolean literal. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "comparison/equals/whole-list-literal-order-and-multiplicity": {
      "status": "unsupported",
      "reason": "Same refusal as `comparison/equals/whole-list-literal`: Chroma $eq takes a scalar literal only, so a whole-list literal has no Where form, and list order and multiplicity are never compared. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "comparison/greater-than/infinity-from-ternary": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated. `allowPostFilter` does not lift it: `div` is applied to two integral literals (the ternary branches), and the plan does not carry whether each was written as an int or a double: `0 / 0` is an error where `0.0 / 0.0` is NaN."
    },
    "comparison/greater-than/nan-from-ternary": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated. `allowPostFilter` does not lift it: `div` is applied to two integral literals (the ternary branches), and the plan does not carry whether each was written as an int or a double: `0 / 0` is an error where `0.0 / 0.0` is NaN."
    },
    "comparison/less-or-equal/nan-from-ternary-on-both-sides": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated. `allowPostFilter` does not lift it: `div` is applied to two integral literals (the ternary branches), and the plan does not carry whether each was written as an int or a double: `0 / 0` is an error where `0.0 / 0.0` is NaN."
    },
    "comparison/less-or-equal/negated-nan-from-ternary-on-both-sides": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `div` is applied to two integral literals (the ternary branches), and the plan does not carry whether each was written as an int or a double: `0 / 0` is an error where `0.0 / 0.0` is NaN."
    },
    "comparison/less-than/nan-from-ternary-value-first": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have neither conditional nor arithmetic operands, so a ternary whose branches are IEEE-754 division results cannot be translated. `allowPostFilter` does not lift it: `div` is applied to two integral literals (the ternary branches), and the plan does not carry whether each was written as an int or a double: `0 / 0` is an error where `0.0 / 0.0` is NaN."
    },
    "comparison/not-equals/whole-list-literal": {
      "status": "unsupported",
      "reason": "ne is unsafe for optional Chroma metadata because missing fields match the filter. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "conditional/ternary/as-whole-exists-body": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "conditional/ternary/inside-all": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "conditional/ternary/inside-exists": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "hierarchy/overlaps/path-built-from-identifier": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no path or prefix operator, and both hierarchy(...) operands are computed — one of them from a list() built around the primary key — so neither side reduces to the bare field or literal a Where clause requires. `allowPostFilter` does not lift it: the post-filter has no evaluation for `list`."
    },
    "hierarchy/overlaps/path-built-from-list-value-first": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `list`."
    },
    "logic/not/deep-mixed-nesting": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `add` meets an integral literal next to a stored number, and the plan does not carry whether it was written as an int or a double: CEL arithmetic has no int/double overload, so `R.attr.x + 1` is an error where `R.attr.x + 1.0` is not."
    },
    "logic/or/comparison-or-exists": {
      "status": "unsupported",
      "reason": "Same missing nested-operand form as `relation/or/two-hops-or-collection-exists`, without the relation hop that masked it: the disjunction's left branch IS a bare boolean metadata field Chroma can take, and the right branch is an exists macro over a collection, which it cannot. A Where clause has no way to hold one of the two, so the whole disjunction is refused. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "logic/or/comparison-or-in-resource-list": {
      "status": "unsupported",
      "reason": "The membership spelling of `logic/or/comparison-or-exists`. Chroma's $in tests a metadata field against a literal LIST; the plan asks the mirror image — whether a literal is a member of a list-valued metadata field — and `$contains` would test that, but the adapter does not translate it (#475): Chroma stores an empty list as an absent key and rejects null elements and mixed types, so the corpus lists cannot be stored as the PDP sees them. So the right branch is refused before the disjunction is built. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "membership/in/field-in-resource-list": {
      "status": "unsupported",
      "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "membership/in/literal-in-resource-number-list": {
      "status": "unsupported",
      "reason": "Value-first membership in a list-valued attribute. The adapter refuses `literal in field` when mirroring it, because Chroma's $in tests a field against a literal list, not the reverse. The pinned stack's list metadata and its $contains cannot stand in for it here: it stores an empty list as an absent key and rejects a null element outright, and these seeds carry both (a2 [], a6 [null, 2]), so the list cannot be stored as the PDP sees it. `allowPostFilter` does not lift it: `R.attr.aNumberList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "membership/in/negated-field-in-resource-list": {
      "status": "unsupported",
      "reason": "Chroma Where documents compare a metadata field against a literal only; there is no field-to-field operand, so a comparison of two metadata keys cannot be translated. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/all/negated-null-element": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/all/negated-null-element-with-false-sibling": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/all/null-element-with-true-sibling": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/equals/field-to-field-both-explicit-null": {
      "status": "unsupported",
      "reason": "Chroma Where documents compare a metadata field against a literal only, so the field-to-field operand is rejected before the null convention on either side is reached — the same limitation as `comparison/equals/field-to-field`. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/equals/negated-explicit-null-against-literal": {
      "status": "unsupported",
      "reason": "The negated-equality spelling lowers to the same $ne absent-key mismatch as `null/not-equals/explicit-null-against-literal`. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/equals/negated-null-literal": {
      "status": "unsupported",
      "reason": "Negating the null equality routes through the same $ne absent-key mismatch as `null/not-equals/null-literal`. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/equals/null-literal": {
      "status": "unsupported",
      "reason": "Chroma $eq accepts only a finite number, string or boolean literal, and Chroma metadata has no null value, so an equality against null cannot be represented. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/equals/null-literal-on-missing-attribute": {
      "status": "unsupported",
      "reason": "Chroma metadata holds only finite numbers, strings and booleans, so a null comparison operand is refused: an absent key cannot be told apart from a stored null. `allowPostFilter` does not lift it: `eq` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "null/exists-one/negated-null-element": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate: $ne/$nin negate a scalar comparison, not a macro, and the adapter refuses to invert one. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/exists/negated-explicit-null-in-principal-list": {
      "status": "unsupported",
      "reason": "The value-list fold puts a collection macro under a negation, and Chroma has no negation for `exists` — the macro is refused before the comparison leaves it folds into are reached, the same limitation as `principal/exists/long-list` reaches from the positive side. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/has-intersection/literal-list-with-null-element": {
      "status": "unsupported",
      "reason": "hasIntersection is not in the adapter's operator set: Chroma has no list-intersection operator, only per-element `$contains`, which the adapter does not translate (#475), and the null element in the literal list has no Chroma form: a null list element is rejected. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/has-intersection/literal-list-with-null-element-against-mapped-names": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/has-intersection/literal-list-with-null-element-against-mapped-names-value-first": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `hasIntersection` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "null/has-intersection/literal-list-with-null-element-value-first": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no hasIntersection operator; the adapter refuses that operation. `allowPostFilter` does not lift it: `hasIntersection` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "null/in/literal-list-of-only-null": {
      "status": "unsupported",
      "reason": "The planner folds membership in [null] to an equality against null; Chroma metadata has no null value, so the comparison cannot be represented. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/in/literal-list-with-null-element": {
      "status": "unsupported",
      "reason": "Chroma metadata has no null — a null value or list element is rejected — so a membership list containing null cannot be represented and the adapter refuses to silently drop the null element. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/in/missing-attribute-in-resource-list": {
      "status": "unsupported",
      "reason": "Chroma metadata filters cannot compare two metadata fields; this shape retains a field on both sides. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/in/negated-explicit-null-in-literal-list": {
      "status": "unsupported",
      "reason": "The negated membership lowers to $nin, which matches records whose metadata key is absent for the same reason $ne does. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/in/negated-literal-list-of-only-null": {
      "status": "unsupported",
      "reason": "Chroma $ne matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/in/negated-literal-list-with-null-element": {
      "status": "unsupported",
      "reason": "Chroma $nin matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/in/negated-missing-attribute-in-resource-list": {
      "status": "unsupported",
      "reason": "Chroma metadata filters cannot compare two metadata fields; this shape retains a field on both sides. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/in/negated-null-literal-in-number-list": {
      "status": "unsupported",
      "reason": "The negation of `null/in/null-literal-in-number-list`: `in` negates to $nin, but the operands still have to be mirrored first, and value-first membership is refused at the same site whatever the polarity. A $nin over a stored list would also match every document missing the key. `allowPostFilter` does not lift it: `in` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "null/in/negated-null-literal-in-resource-list": {
      "status": "unsupported",
      "reason": "Chroma's $in tests a metadata field against a literal list, not whether a literal is a member of a list-valued metadata field. `$contains` would test that, but the adapter does not translate it (#475): Chroma stores an empty list as an absent key and rejects null elements and mixed types, so the corpus lists cannot be stored as the PDP sees them. `allowPostFilter` does not lift it: `in` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "null/in/null-literal-in-number-list": {
      "status": "unsupported",
      "reason": "Value-first membership in a list-valued attribute, refused when mirroring the operands as `membership/in/literal-in-resource-number-list` is: Chroma's $in tests a field against a literal list, not the reverse. The null literal is not what stops it, but it could not be answered either: Chroma metadata has no null value, and the pinned stack's list metadata rejects a6's [null, 2] outright, so the one row the oracle allows cannot be stored as the PDP sees it. `allowPostFilter` does not lift it: `in` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "null/in/null-literal-in-resource-list": {
      "status": "unsupported",
      "reason": "Chroma's $in tests a metadata field against a literal list, not whether a literal is a member of a list-valued metadata field. `$contains` would test that, but the adapter does not translate it (#475): Chroma stores an empty list as an absent key and rejects null elements and mixed types, so the corpus lists cannot be stored as the PDP sees them. `allowPostFilter` does not lift it: `in` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "null/not-equals/explicit-null-against-literal": {
      "status": "unsupported",
      "reason": "Chroma $ne matches records whose metadata key is absent, and `owner` is stored as absent for a NULL column because Chroma metadata has no null value. The explicit-null convention this action probes therefore cannot even be represented in the store, let alone compared. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/not-equals/field-to-field-mixed-null-conventions": {
      "status": "unsupported",
      "reason": "A Chroma `Where` clause compares one metadata key with a literal; it has no form that compares two keys. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/not-equals/null-literal": {
      "status": "unsupported",
      "reason": "Chroma $ne matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics, and this field is not declared required. `allowPostFilter` does not lift it: `R.attr.owner` reaches `check()` as an explicit null on some rows, and Chroma metadata has no null, so the harness stores it nowhere, the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "null/not-equals/null-literal-value-first": {
      "status": "unsupported",
      "reason": "Value-first form of `null/not-equals/null-literal`; the same $ne absent-key mismatch applies after the planner mirrors the operands. `allowPostFilter` does not lift it: `ne` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "principal/except/size-after-removing-resource-value": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `except`."
    },
    "principal/exists/list-of-structs": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `list`."
    },
    "principal/exists/list-of-structs-with-null-field": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `list`."
    },
    "principal/exists/shadowed-lambda-variable": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "regex/matches/bounded-repetition": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/brace-without-count-is-literal": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/case-fold-kelvin-and-long-s": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/case-insensitive-flag": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/digit-class": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/dot-excludes-newline": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/dot-plus-excludes-empty-and-newline": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/grouped-alternation": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/literal-brace": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/lookahead-from-principal": {
      "status": "unsupported",
      "reason": "Chroma metadata filters expose neither computed scalar expressions nor regex matching. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/lucene-reserved-characters-in-class": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/negated-invalid-patterns-from-principal": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator, so there is nothing to negate: `matches` is not in the adapter's negation table because it is not expressible positively either. The invalid patterns and the aNumber == 5 disjunct are refused with it. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/posix-character-class": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/posix-class-and-range-bounded": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/top-level-alternation-prefix-or-suffix": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no regular-expression operator. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/top-level-alternation-single-characters": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/unanchored-quantifiers": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "regex/matches/whitespace-class": {
      "status": "unsupported",
      "reason": "Chroma metadata filters have no matches operator; the adapter refuses that operation. `allowPostFilter` does not lift it: the pattern is outside the literal, anchor and trailing `.*` subset the post-filter answers with plain string comparisons, and a JavaScript regex engine is not RE2."
    },
    "relation/all/negated-to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate, and no relation model to reach the chained collection with in the first place — the same limitation as `relation/exists/negated-to-one-chain`, reached through all(). `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/all/to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, so the chained collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/exists/negated-to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma has no negation of a collection predicate, and no relation model to reach the chained collection with in the first place. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/exists/to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no quantifier over a list's elements and no relation or array-of-object model, and every comparison operand must be a bare field or literal, so the plan's collection lambda cannot be translated. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/has-intersection/negated-empty-list-on-to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma metadata filters cannot negate the retained hasIntersection expression; there is no `$not`, and the macro has no Where form to invert. `allowPostFilter` does not lift it: `R.attr.mainCategory.subNames` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/has-intersection/negated-to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma has no operator that intersects a list-valued metadata field with a literal list, so hasIntersection has no Where equivalent to negate; the adapter refuses to invert it. `allowPostFilter` does not lift it: `R.attr.mainCategory.subNames` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/in/negated-to-one-chain": {
      "status": "unsupported",
      "reason": "Same literal-in-field limitation as `relation/in/to-one-chain`, reached under a negation: the adapter rejects the membership direction before the negation is even considered. `allowPostFilter` does not lift it: `R.attr.mainCategory.subNames` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/in/to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma's $in tests a metadata field against a literal list, not whether a literal is a member of a list-valued metadata field. `$contains` would test that, but the adapter does not translate it (#475): Chroma stores an empty list as an absent key and rejects null elements and mixed types, so the corpus lists cannot be stored as the PDP sees them. `allowPostFilter` does not lift it: `R.attr.mainCategory.subNames` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/not-equals/one-hop-null-literal": {
      "status": "unsupported",
      "reason": "`!= null` over the chain key parent.aOptionalString lowers to $ne, and a Chroma $ne matches a document missing that key, where CEL denies on the missing-attribute error; a null literal has no other Chroma spelling, since Chroma metadata has no null. Both flavours of absence — no parent at all, and a parent whose optional column is NULL — are stored as a missing key, so there is nothing for Chroma to distinguish them with. `allowPostFilter` does not lift it: `ne` compares a null literal, and Chroma metadata has no null, so a record cannot tell an explicit null attribute from a missing one."
    },
    "relation/or/two-hops-or-collection-exists": {
      "status": "unsupported",
      "reason": "Every Chroma comparison operand must be a bare metadata field or a literal, and the disjunction's second branch is an exists macro over a relation — a nested expression the Where model has no form for. The two-level hop in the first branch is never reached. `allowPostFilter` does not lift it: `R.attr.categories` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/size/negated-non-empty-to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma comparison operands must be a bare field or literal, so the negated size(...) collection count is rejected as a nested expression — the same missing relation model and count function as `relation/size/zero-to-one-chain`. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/size/non-empty-to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/size/non-negative-to-one-chain": {
      "status": "unsupported",
      "reason": "Same missing relation model and count function as `relation/size/zero-to-one-chain`. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/size/zero-to-one-chain": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "relation/ternary/to-one-chain-condition": {
      "status": "unsupported",
      "reason": "Same three-operand conditional as `conditional/ternary/boolean-branches`: Chroma's Where model takes only two-operand field-vs-literal comparisons, so the ternary is rejected at the top of the filter before its chained condition is examined. `allowPostFilter` does not lift it: `R.attr.mainCategory.subNames` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/equals/filtered-collection": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/equals/negated-collection-zero": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/greater-or-equal/collection-at-least-one": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/greater-or-equal/fractional-threshold": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/greater-or-equal/fractional-threshold-on-relation": {
      "status": "unsupported",
      "reason": "Same missing relation model and count function as `relation/size/zero-to-one-chain`, reached through a fractional threshold. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/greater-than/collection-above-one": {
      "status": "unsupported",
      "reason": "Chroma's Where grammar has no count function — a stored list's length cannot be read, and an empty list is not stored at all (its key is dropped) — and no relation model, so a size(...) collection-count operand cannot be translated. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/less-or-equal/fractional-threshold-on-relation": {
      "status": "unsupported",
      "reason": "Same missing relation model and count function as `size/greater-or-equal/fractional-threshold-on-relation` under the opposite ordering. `allowPostFilter` does not lift it: `R.attr.mainCategory.subCategories` is a list reached through a relation, which Chroma metadata has no model for, so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "size/less-than/value-first": {
      "status": "unsupported",
      "reason": "size() over a collection is a computed operand with no Chroma equivalent — there is no count function and no relation to count — so it is refused as a nested expression, in this operand position as in the collection-first one. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/equals/boolean-list-element-against-number-literal": {
      "status": "unsupported",
      "reason": "Same missing positional access as `collection/index/first-element-of-boolean-list`, in the left disjunct. A Where clause cannot hold one disjunct without the other, so the translatable aNumber == 5 branch is refused with it, and the cross-type question the probe asks is never reached. `allowPostFilter` does not lift it: `R.attr.aBoolList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/equals/negated-on-missing-attribute": {
      "status": "unsupported",
      "reason": "The negated equality lowers to $ne on aDouble, which is missing on a3, and Chroma $ne matches records whose metadata key is absent, unlike Cerbos missing-attribute error semantics. The second disjunct's string() cast has no Where form either. `allowPostFilter` does not lift it: `string()` reads a number from metadata, and the chromadb client stores metadata through JSON, which writes -0.0 as 0, so a stored zero's sign is lost where `string()` renders -0.0 as \"-0\"."
    },
    "type-mismatch/equals/number-list-element-against-boolean-literal": {
      "status": "unsupported",
      "reason": "The mirror of `type-mismatch/equals/boolean-list-element-against-number-literal`: the index() into a number list in the left disjunct is refused as a nested expression, and the whole disjunction with it. `allowPostFilter` does not lift it: `R.attr.aNumberList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/equals/string-field-against-map-literal": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `struct`."
    },
    "type-mismatch/equals/string-field-against-map-literal-with-null-value": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `struct`."
    },
    "type-mismatch/exists/element-field-against-number-literal": {
      "status": "unsupported",
      "reason": "The exists() over the tags collection arrives as a lambda operand, which a Chroma `Where` clause has no form for: tags are not stored as metadata at all, and there is no per-element predicate to fold the lambda into. The cross-type question the probe asks (a string name against 0) is never reached, and a disjunction with one untranslatable branch is refused whole, so the aNumber == 5 branch goes with it. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/greater-or-equal/negated-explicit-null-field-against-string-literal": {
      "status": "unsupported",
      "reason": "Chroma's $lt/$lte/$gt/$gte compare numeric metadata only, so an ordering of the owner key against a string literal is refused; Chroma cannot order strings at all"
    },
    "type-mismatch/greater-than/string-literal-against-explicit-null-field-value-first": {
      "status": "unsupported",
      "reason": "Chroma's $lt/$lte/$gt/$gte compare numeric metadata only, so an ordering of the owner key against a string literal is refused; Chroma cannot order strings at all"
    },
    "type-mismatch/has-intersection/mapped-names-against-mixed-literal-list": {
      "status": "unsupported",
      "reason": "The map() projection of tag names is a nested expression in an operand position, refused before the mixed literal list [\"public\", 0] is examined; Chroma has no relation to project from and no list-valued field for a set intersection to test. `allowPostFilter` does not lift it: `R.attr.tags` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/has-intersection/resource-boolean-list-against-string-literal-list": {
      "status": "unsupported",
      "reason": "Same missing hasIntersection operator as `type-mismatch/has-intersection/resource-number-list-against-mixed-literal-list`, over a boolean list, in the left disjunct; the whole disjunction is refused with it, so the aNumber == 5 branch is not emitted either. `allowPostFilter` does not lift it: `R.attr.aBoolList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/has-intersection/resource-number-list-against-mixed-literal-list": {
      "status": "unsupported",
      "reason": "hasIntersection is not in the adapter's operator set: the pinned stack stores an empty list as an absent key and rejects the null elements these seeds carry, so their lists cannot be stored as the PDP sees them. The walk refuses the operator before the mixed literal list [\"2\", 3] is examined. `allowPostFilter` does not lift it: `R.attr.aNumberList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/has-intersection/resource-string-list-against-map-element": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/in/list-literal-in-resource-string-list": {
      "status": "unsupported",
      "reason": "ChromaDB filters cannot test whether a literal is contained in a metadata field. `allowPostFilter` does not lift it: `R.attr.tagNames` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/in/string-field-in-list-of-lists": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `list`."
    },
    "type-mismatch/in/string-literal-in-resource-boolean-list": {
      "status": "unsupported",
      "reason": "The boolean mirror of `type-mismatch/in/string-literal-in-resource-number-list`: value-first membership refused when mirroring the operands, taking the aNumber == 5 disjunct with it. The pinned server's $contains does not match \"true\" against a stored [true], but rejects a4's [null, true] and stores the empty lists as absent keys. `allowPostFilter` does not lift it: `R.attr.aBoolList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/in/string-literal-in-resource-number-list": {
      "status": "unsupported",
      "reason": "Same value-first membership refusal as `membership/in/literal-in-resource-number-list`, in the left disjunct; the whole disjunction is refused with it, so the aNumber == 5 branch is not emitted either. The cross-type hazard is not what stops it: the pinned server's $contains is type-exact (\"2\" does not match a stored [2]), so a lowering would not coerce, but the seeds' empty lists and null elements still cannot be stored. `allowPostFilter` does not lift it: `R.attr.aNumberList` is a list, which the harness does not store as Chroma metadata (#475), so the mapping declares no key for it and the post-filter reads only declared keys."
    },
    "type-mismatch/less-than/explicit-null-field-against-string-literal": {
      "status": "unsupported",
      "reason": "Chroma's $lt/$lte/$gt/$gte compare numeric metadata only, so an ordering of the owner key against a string literal is refused; Chroma cannot order strings at all"
    },
    "type-mismatch/less-than/negated-nan-against-string-field": {
      "status": "unsupported",
      "reason": "Chroma metadata filters cannot evaluate the retained ternary and arithmetic operands of this comparison. `allowPostFilter` does not lift it: `div` is applied to two integral literals (the ternary branches), and the plan does not carry whether each was written as an int or a double: `0 / 0` is an error where `0.0 / 0.0` is NaN."
    },
    "type-mismatch/not-equals/string-field-against-map-literal": {
      "status": "unsupported",
      "reason": "Chroma metadata filters compare a field to a literal and cannot evaluate the computed expression this planner shape retains. `allowPostFilter` does not lift it: the post-filter has no evaluation for `struct`."
    }
  }
}
