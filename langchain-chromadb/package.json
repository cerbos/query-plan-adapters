{
  "name": "@cerbos/langchain-chromadb",
  "version": "0.2.0",
  "homepage": "https://cerbos.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/cerbos/query-plan-adapters",
    "directory": "packages/langchain-chromadb"
  },
  "bugs": {
    "url": "https://github.com/cerbos/query-plan-adapters/issues"
  },
  "author": "Cerbos <help@cerbos.dev> (https://cerbos.dev)",
  "license": "Apache-2.0",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "files": [
    "lib/**/*.d.ts",
    "lib/**/*.d.ts.map",
    "lib/**/*.js",
    "lib/**/*.js.map",
    "src/**/*.ts",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "//test": "`test` is the offline unit suite: caller-supplied options, the refusal type and malformed plans. It needs no Cerbos and no ChromaDB. `test:adversarial` replays the recorded conformance goldens (../conformance/golden/) against a real ChromaDB (`npm run chroma`, on port 8234; override with CHROMA_URL) and needs no PDP.",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "prepare": "npm run build",
    "preversion": "npm run lint",
    "version": "git add -A src",
    "postversion": "git push && git push --tags",
    "test": "jest",
    "test:adversarial": "jest --config jest.adversarial.config.js",
    "chroma": "docker run --rm -p 8234:8000 \"$(cat CHROMA_IMAGE)\""
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@cerbos/core": "^0.33.0",
    "@jest/globals": "^30.2.0",
    "@tsconfig/node24": "^24.0.0",
    "@tsconfig/strictest": "^2.0.8",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.0",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.4.0",
    "typescript": "^6.0.0"
  },
  "peerDependencies": {
    "@cerbos/core": "^0.32.0 || ^0.33.0"
  },
  "dependencies": {
    "chromadb": "^3.2.2"
  }
}
