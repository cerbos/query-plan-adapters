# Profile for DemoApplication, which activates it itself. Everything else, including
# cerbos.address, comes from application.yaml.
spring:
  datasource:
    # Its own database name, separate from the photo-sharing app's `photos`.
    url: jdbc:h2:mem:demo;DB_CLOSE_DELAY=-1
