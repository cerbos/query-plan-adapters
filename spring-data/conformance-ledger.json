{
  "adapter": "spring-data",
  "cases": {
    "arithmetic/divide/field-by-field": {
      "status": "unsupported",
      "reason": "A column divisor that may hold a signed zero: CEL divides a non-zero dividend by -0.0 into -Infinity and by 0.0 into +Infinity, and SQL cannot read the sign of a stored zero (-0.0 = 0.0 holds), so a comparison the two infinities decide differently is refused."
    },
    "arithmetic/divide/int-truncates": {
      "status": "unsupported",
      "reason": "int() is solved only against a constant (int(column) == literal), never inside arithmetic: CEL int division truncates toward zero, and a JPA division over the column has neither int() nor that truncation, so int(aNumber) / 2 is refused."
    },
    "arithmetic/divide/int-truncates-toward-zero": {
      "status": "unsupported",
      "reason": "Same refusal as arithmetic/divide/int-truncates: int() inside arithmetic has no JPA Criteria form that truncates toward zero as CEL int division does."
    },
    "arithmetic/divide/negated-int-truncates": {
      "status": "unsupported",
      "reason": "Same refusal as arithmetic/divide/int-truncates, under negation: int() inside arithmetic has no JPA Criteria form that truncates toward zero as CEL int division does."
    },
    "arithmetic/divide/nested-division-divisor": {
      "status": "unsupported",
      "reason": "The divisor aDouble / aNumber is zero with the sign of aDouble's zero, and CEL's quotient is an infinity of that sign; SQL cannot read the sign of a stored zero (-0.0 = 0.0 holds), so a comparison the two infinities decide differently is refused."
    },
    "arithmetic/modulo/column-divisor-negative-and-zero": {
      "status": "unsupported",
      "reason": "SQL MOD matches CEL's truncating % only between int() of an Integer column and an integral constant; a divisor that is itself arithmetic (int(aNumber) - 5) has no faithful int() lowering, since CAST rounds where CEL truncates toward zero."
    },
    "arithmetic/modulo/negated-double-operand": {
      "status": "unsupported",
      "reason": "CEL % has no double overload, so aNumber % 2 is an error on every row; the adapter lowers % only between int() of an Integer column and an integral constant and refuses the rest rather than let a store modulo turn the error into a negatable value."
    },
    "arithmetic/multiply/nan-times-zero-plus-column": {
      "status": "unsupported",
      "reason": "A division that may be 0/0 composed into further arithmetic: CEL carries the NaN through the multiplication and addition, while SQL has no NaN and the NULLIF zero guard yields NULL, so the shape is refused."
    },
    "cast/double/malformed-string": {
      "status": "unsupported",
      "reason": "SQL CAST reads a numeric prefix where CEL requires the whole string and raises otherwise, so double() over a string column is refused rather than lowered to CAST."
    },
    "cast/double/negated-malformed-string": {
      "status": "unsupported",
      "reason": "The Criteria API has no portable CEL numeric parsing expression that preserves conversion errors under negation."
    },
    "cast/int/string-beyond-double-precision": {
      "status": "unsupported",
      "reason": "The plan carries the int literal as a double, which at 2^53 and beyond no longer pins a single integer, so int() compared with such a bound is refused rather than compared through double arithmetic."
    },
    "cast/string/from-int-beyond-double-precision": {
      "status": "unsupported",
      "reason": "string() is solved only over a mapped attribute compared with a constant. string(int(column)) nests a conversion JPA Criteria cannot spell: int() parses the whole string exactly to int64, and string() renders the int in plain decimal."
    },
    "cast/string/from-int-past-exponent-threshold": {
      "status": "unsupported",
      "reason": "string() is solved only over a mapped attribute compared with a constant. string(int(column)) nests a conversion JPA Criteria cannot spell: int() truncates the double toward zero, and string() renders the int in plain decimal (\"1000000\", not the double's \"1e+06\")."
    },
    "cast/string/from-negative-zero-double": {
      "status": "unsupported",
      "reason": "string() of a double compared with \"-0\": SQL cannot read the sign of a stored zero (-0.0 = 0.0 holds, and the stores print it without the sign), so the rendering CEL gives -0.0 cannot be told from 0.0."
    },
    "cast/string/negated-from-boolean-expression": {
      "status": "unsupported",
      "reason": "string() is solved only over a mapped attribute compared with a constant. A boolean-valued comparison is not an attribute, and JPA Criteria has no boolean-to-text conversion that renders CEL's \"true\"/\"false\" on every store."
    },
    "cast/string/negated-from-boolean-ternary": {
      "status": "unsupported",
      "reason": "string() is solved only over a mapped attribute compared with a constant. A ternary is not an attribute, and JPA Criteria has no boolean-to-text conversion that renders CEL's \"true\"/\"false\" on every store."
    },
    "cast/string/negated-from-int-past-exponent-threshold": {
      "status": "unsupported",
      "reason": "Same refusal as cast/string/from-int-past-exponent-threshold, under negation: string(int(column)) has no JPA Criteria form."
    },
    "cast/timestamp/malformed-string": {
      "status": "unsupported",
      "reason": "timestamp() over createdBy, which is mapped to a String column: a String does not pin an absolute instant, and a malformed value must stay a CEL error, which no SQL cast preserves, so the adapter refuses to compare it."
    },
    "cast/timestamp/negated-malformed-string": {
      "status": "unsupported",
      "reason": "createdBy is stored as a String; timestamp comparisons require an absolute-instant Java column type, rather than lexicographic string ordering or database-specific parsing."
    },
    "collection/except/as-whole-condition": {
      "status": "unsupported",
      "reason": "except() returns a list, not a boolean, so in boolean position it has no Criteria meaning and is refused; it translates only under size() or compared with a list of at most one element."
    },
    "collection/exists/map-keys": {
      "status": "unsupported",
      "reason": "A macro over a map ranges over its keys, and the to-one parent is mapped per field (parent.aBool, parent.aString, ...), not as a whole: a JPA relation row has no key set to iterate, so R.attr.parent itself is an unmapped attribute."
    },
    "collection/exists/negated-map-keys": {
      "status": "unsupported",
      "reason": "Same refusal as collection/exists/map-keys, under negation: the to-one parent is mapped per field, and a JPA relation row has no key set to range over."
    },
    "collection/filter/as-conjunct": {
      "status": "unsupported",
      "reason": "filter() returns a list, not a boolean; only size(filter(...)) has a Criteria meaning, so a filter() conjunct is refused rather than dropped (dropping it would over-grant)."
    },
    "collection/filter/as-whole-condition": {
      "status": "unsupported",
      "reason": "filter() returns a list, not a boolean; only size(filter(...)) has a Criteria meaning, so a bare filter() in boolean position is refused."
    },
    "collection/map/as-whole-condition": {
      "status": "unsupported",
      "reason": "map() returns a list, not a boolean; it translates as the collection of hasIntersection, in list equality and in membership, and used as the whole condition it is refused."
    },
    "comparison/less-than/astral-string-against-high-bmp": {
      "status": "unsupported",
      "reason": "CEL orders strings by code point; H2 compares VARCHAR by UTF-16 code unit (Java's String.compareTo), which sorts a6's astral U+1F680 (high surrogate 0xD83D) before U+FFFD. An ordering against a literal holding a code unit at or above U+D800 is refused, since only such a literal can be ordered differently."
    },
    "comparison/less-than/negated-astral-string-against-high-bmp": {
      "status": "unsupported",
      "reason": "Same refusal as comparison/less-than/astral-string-against-high-bmp, under negation: a UTF-16 code unit ordering puts the surrogate pair before U+FFFD, so the literal is refused."
    },
    "regex/matches/unanchored-quantifiers": {
      "status": "unsupported",
      "reason": "JPA Criteria has no RE2 predicate, so matches() lowers to LIKE only where LIKE can spell the pattern exactly; the unbounded repetitions x* and a+ have no LIKE spelling (register an OperatorFunction for matches to use a database regex)."
    },
    "timestamp/equals/field-to-field-without-conversion": {
      "status": "unsupported",
      "reason": "Bare temporal comparison cannot preserve CEL string equality; use timestamp() explicitly"
    }
  }
}
