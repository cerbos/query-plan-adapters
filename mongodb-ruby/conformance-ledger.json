{
  "adapter": "mongodb-ruby",
  "cases": {
    "cast/string/from-integral-ternary": {
      "status": "unsupported",
      "reason": "The ternary's branches are int literals, which CEL renders in plain decimal (\"1000000\"), but the plan ships an int and a double constant as the same bare number and $toString over a double renders the exponent form (\"1e+06\"), so the adapter refuses string() over an untyped integral constant of 1e6 or more"
    },
    "cast/string/negated-from-integral-ternary": {
      "status": "unsupported",
      "reason": "Same untyped integral constant under string() as `cast/string/from-integral-ternary`, under negation"
    },
    "collection/exists/map-keys": {
      "status": "unsupported",
      "reason": "The macro ranges over a to-one relation, which CEL reads as a map and iterates by KEY. The keys are the attribute names the application sends, which the stored subdocument does not carry: $objectToArray yields stored field names, which the mapping may rename, a null a nullable field stores is a key MongoDB keeps and CEL never sees, and a nested relation (parent.inner) is a separate mapper entry. No expression reads the key set without guessing it."
    },
    "collection/exists/negated-map-keys": {
      "status": "unsupported",
      "reason": "Same key iteration over a to-one relation as `collection/exists/map-keys`, under negation."
    },
    "membership/in/map-attribute-keys": {
      "status": "unsupported",
      "reason": "`in` over a to-one relation tests the related map's KEYS, the attribute names the application sends, which the stored subdocument does not carry (see `collection/exists/map-keys`: renamed fields, stored nulls CEL never sees, nested relations mapped separately)."
    },
    "membership/in/negated-map-attribute-keys": {
      "status": "unsupported",
      "reason": "Same key membership over a to-one relation as `membership/in/map-attribute-keys`, under negation"
    },
    "timestamp/equals/field-to-field-without-conversion": {
      "status": "unsupported",
      "reason": "A bare comparison of two date_time fields: CEL compares the strings the application sent, and a stored BSON date has discarded the lexical spelling (two spellings of one instant are equal dates and unequal strings). Refused in the filter and in the three-valued evaluation alike; compare timestamp(...) values instead."
    }
  }
}
