// Copyright 2021-2026 Zenauth Ltd.
// SPDX-License-Identifier: Apache-2.0

// Package ent is the generated client for the demo domain's one entity.
//
// Everything beside this file is written by the generator and committed, which is ent's own
// convention: a consumer's repository holds its generated client, and holding it here means this
// example builds, vets and lints straight from a checkout with no generation step. Regenerate with
// `go generate ./ent` after editing schema/document.go — the generator is pinned as a tool
// dependency in go.mod, so the version that ran is the version this module declares.
package ent

//go:generate go tool ent generate ./schema
