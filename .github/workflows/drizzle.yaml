name: Drizzle Test

on:
  pull_request:
    paths:
      - "drizzle/**"
      - "policies/**"
      - ".github/workflows/drizzle.yaml"
      - ".github/workflows/drizzle-publish.yaml"
  push:
    tags:
      - drizzle/v*

defaults:
  run:
    working-directory: drizzle

jobs:
  test:
    strategy:
      matrix:
        cerbos-version: ["latest"]
        node-version: ["20", "22", "24", "25"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Cerbos
        uses: cerbos/cerbos-setup-action@f8061b1f07da2d185acd233dedaa5d5eadb99214 # v1
        with:
          version: ${{ matrix.cerbos-version }}

      - name: Test using Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ matrix.node-version }}

      - run: npm install
      - run: npm run test
